<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>关于Mysql的truncate操作</title>
      <link href="/mysql-truncate.html"/>
      <url>/mysql-truncate.html</url>
      
        <content type="html"><![CDATA[<p>在公司项目的旧代码中看到有mysql的truncate操作。<br>逻辑就是truncate操作之后，执行任务A，如果A出错，则进行rollback回滚。</p><p>应该是最近同事写需求，修改了A，然后某种条件A报错了，然后操作的那张表发现所有的数据都没了。</p><p>来了解一下truncate的操作。</p><a id="more"></a><h2 id="TRUNCATE"><a href="#TRUNCATE" class="headerlink" title="TRUNCATE"></a>TRUNCATE</h2><p><code>truncate</code>是对数据进行删除的操作，可能会想到delete以及drop，通过比较三者来说说他们的区别。</p><p><code>delete</code>语句执行删除是每次从表中删除一行，并且将此次删除操作作为事物记录在日志中保存以便进行回滚操作。</p><p><code>truncate</code>则是一次性的删除表中所有的数据，而且不会将操作记录计入日志保存，通过再<code>insert</code>数据比较与<code>delete</code>的区别就是：<code>delete</code>之后再插入数据，自增的id是以删除之前的id开始增长的，但是<code>truncate</code>之后，是从0开始的。当表被TRUNCATE 后，这个表和索引所占用的空间会恢复到初始大小。</p><blockquote><p>truncate会删除表中所有记录，并且将重新设置高水线和所有的索引，缺省情况下将空间释放到minextents个extent，除非使用reuse storage，。不会记录日志，所以执行速度很快，但不能通过rollback撤消操作（如果一不小心把一个表truncate掉，也是可以恢复的，只是不能通过rollback来恢复）。</p></blockquote><p>所以对于存在外键约束的表，不能使用truncate，而应该使用不带where的delete。由于 TRUNCATE TABLE 不记录在日志中，所以它不能激活触发器。</p><p>drop语句将表所占用的空间全释放掉。包括这个表。</p>]]></content>
      
      
      <categories>
          
          <category> Mysql </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Mysql </tag>
            
            <tag> 删库 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>python中的四舍五入</title>
      <link href="/python-round.html"/>
      <url>/python-round.html</url>
      
        <content type="html"><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>最近工作过程中遇到了需要计算金钱的问题，由于有零有整，多次计算之后进行保留两位小数的时候，会发生计算不太准确的问题。<br>比如：<br><code>572.725</code>需要保留两位小数，按照我们生活中来说，应该是<code>572.73</code>，但是在程序中最后保留的结果为<code>572.72</code>。</p><p>通过查资料，多数编程语言中，采用的不是我们生活中常用“四舍五入”，而是让统计数据更公平，降低舍入的误差的“奇进偶舍”的取舍方式，又称为“四舍六入五成双”。</p><a id="more"></a><h2 id="奇进偶舍（四舍六入五成双）"><a href="#奇进偶舍（四舍六入五成双）" class="headerlink" title="奇进偶舍（四舍六入五成双）"></a>奇进偶舍（四舍六入五成双）</h2><p>“奇进偶舍”是一种比较精确比较科学的计数保留法，是一种数字修约规则。从另一种说法“四舍六入五成双”来看，就是遇到六才进一位，而遇到五需要进行别的操作。</p><p>举个例子🌰：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">a = <span class="number">2.724</span></span><br><span class="line">b = <span class="number">2.725</span></span><br><span class="line">c = <span class="number">2.726</span></span><br><span class="line">d = <span class="number">2.735</span></span><br><span class="line"><span class="comment"># 上面四个数字，保留两位小数的结果</span></span><br><span class="line"><span class="comment"># a = 2.72 最后一位是4，舍掉</span></span><br><span class="line"><span class="comment"># b = 2.72 最后一位是5，但是上一位是2，偶数，则不进1</span></span><br><span class="line"><span class="comment"># c = 2.73 最后一位是6，进1</span></span><br><span class="line"><span class="comment"># d = 2.74 最后一位是5，上一位是3，奇数，进1</span></span><br></pre></td></tr></table></figure><p><strong>四舍六入五成双，高位为单数则进1凑成双数，高位为双数则不进位。</strong></p><p>至于为什么要这么算，看一个百度百科说的例子：</p><p>1.15+1.25+1.35+1.45=5.2，若按四舍五入取一位小数计算：</p><p>1.2+1.3+1.4+1.5=5.4</p><p>按“四舍六入五成双”计算，1.2+1.2+1.4+1.4=5.2，舍入后的结果更能反映实际结果。</p><p>至于更深的原因，也没有去详细了解了，有兴趣的可以自己去查查看。下面来说说如果遇到了需要四舍五入的情况，需要怎么办。</p><h2 id="Decimal"><a href="#Decimal" class="headerlink" title="Decimal"></a>Decimal</h2><p>工作中总会遇到需要进行浮点数计算的时候，但是我们直接写在程序里面的浮点数，计算机是无法精确的使用二进制小数来进行表示。可以看一篇大佬的文章<a href="https://mp.weixin.qq.com/s?__biz=MzAxOTc0NzExNg==&mid=2665513140&idx=1&sn=565517e977ac56904305a4a9f9d65012&scene=38#wechat_redirect" target="_blank" rel="noopener">浮点数为什么不精确？</a>，受益良多。</p><p>不知道在别的编程语言怎么处理，在python中通常是使用标准库<code>Decimal</code>来处理的。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> decimal <span class="keyword">import</span> Decimal</span><br><span class="line"></span><br><span class="line">a = <span class="number">1.23</span></span><br><span class="line">b = <span class="number">2.3434</span></span><br><span class="line"><span class="comment"># 先转换为str，不然还是会有计算不精确的情况发生。</span></span><br><span class="line"><span class="comment"># Decimal('3.573399999999999909761072558')</span></span><br><span class="line">c = Decimal(str(a)) + Decimal(str(b))  </span><br><span class="line">print(c)</span><br><span class="line"><span class="comment"># Decimal('3.5734')</span></span><br></pre></td></tr></table></figure><p>需要进行小数位的保留的时候，使用<code>quantize()</code>方法。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> decimal</span><br><span class="line"><span class="keyword">from</span> decimal <span class="keyword">import</span> Decimal</span><br><span class="line">c = Decimal(<span class="string">'3.5734'</span>)</span><br><span class="line">d = c.quantize(Decimal(<span class="string">".00"</span>))  <span class="comment"># 这就是保留两位小数操作</span></span><br></pre></td></tr></table></figure><p><code>quantize()</code>提供了一个rounding参数<code>rounding</code>来决定在保留两位小数的时候进行的操作。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 这是源码中的内容</span></span><br><span class="line"><span class="comment"># Rounding</span></span><br><span class="line">ROUND_DOWN = <span class="string">'ROUND_DOWN'</span></span><br><span class="line">ROUND_HALF_UP = <span class="string">'ROUND_HALF_UP'</span></span><br><span class="line">ROUND_HALF_EVEN = <span class="string">'ROUND_HALF_EVEN'</span></span><br><span class="line">ROUND_CEILING = <span class="string">'ROUND_CEILING'</span></span><br><span class="line">ROUND_FLOOR = <span class="string">'ROUND_FLOOR'</span></span><br><span class="line">ROUND_UP = <span class="string">'ROUND_UP'</span></span><br><span class="line">ROUND_HALF_DOWN = <span class="string">'ROUND_HALF_DOWN'</span></span><br><span class="line">ROUND_05UP = <span class="string">'ROUND_05UP'</span></span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> decimal <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    a = Decimal(<span class="string">'3.1415926535'</span>)</span><br><span class="line">    print(a)</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>)))</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>), rounding=ROUND_05UP))</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>), rounding=ROUND_CEILING))</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>), rounding=ROUND_DOWN))</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>), rounding=ROUND_UP))</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>), rounding=ROUND_FLOOR))</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>), rounding=ROUND_HALF_DOWN))</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>), rounding=ROUND_HALF_UP))</span><br><span class="line">    print(a.quantize(Decimal(<span class="string">"1.000"</span>), rounding=ROUND_HALF_EVEN))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出如下：</span></span><br><span class="line"><span class="comment"># 3.1415926535</span></span><br><span class="line"><span class="comment"># 3.142</span></span><br><span class="line"><span class="comment"># ROUND_05UP： 3.141 不进位</span></span><br><span class="line"><span class="comment"># ROUND_CEILING： 3.142  CEILING正数始终进位</span></span><br><span class="line"><span class="comment"># ROUND_DOWN： 3.141  不进位</span></span><br><span class="line"><span class="comment"># ROUND_UP： 3.142  进位</span></span><br><span class="line"><span class="comment"># ROUND_FLOOR： 3.141  FLOOR正数始终不进位</span></span><br><span class="line"><span class="comment"># ROUND_HALF_DOWN： 3.142  不进位</span></span><br><span class="line"><span class="comment"># ROUND_HALF_UP： 3.142  进位</span></span><br><span class="line"><span class="comment"># ROUND_HALF_EVEN： 3.142  四舍五入</span></span><br></pre></td></tr></table></figure><p>所以在使用的时候可以根据自己的需求来选择rounding的值。</p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> Decimal </tag>
            
            <tag> 浮点数计算 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>冒泡排序</title>
      <link href="/bubble-sort.html"/>
      <url>/bubble-sort.html</url>
      
        <content type="html"><![CDATA[<p>冒泡排序是一种比较简单的排序方法。通过比较两个相邻的元素，如果他们的顺序有误，就把他们交换顺序。</p><p>比如要将一个列表进行排序，就是对该列表进行遍历，将每次拿到的元素与其他元素进行比较交换位置，每一次遍历只排好一个数字。</p><p>下面看看图示：</p><a id="more"></a><p>一次遍历：</p><p><img src="https://i.loli.net/2019/06/26/5d1328e72991010400.png" alt="loop"></p><p>后续的遍历也只是循环这样的操作，就不再用图解释了。</p><p>看看python实现的<a href="https://github.com/ZhangzhiS/study_note/blob/master/bubble_sort/bubble_sort.py" target="_blank" rel="noopener">代码</a>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">bubble_sort</span><span class="params">(array: list)</span> -&gt; list:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    冒泡排序</span></span><br><span class="line"><span class="string">    冒泡排序要排序n个数，由于每遍历一趟只排好一个数字，</span></span><br><span class="line"><span class="string">    则需要遍历n-1趟，所以最外层循环是要循环n-1次，</span></span><br><span class="line"><span class="string">    而每次趟遍历中需要比较每归位的数字，</span></span><br><span class="line"><span class="string">    则要在n-1次比较中减去已排好的i位数字，</span></span><br><span class="line"><span class="string">    则第二层循环要遍历是n-1-i次</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    array_len = len(array)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(array_len - <span class="number">1</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(array_len - <span class="number">1</span> - i):</span><br><span class="line">            <span class="keyword">if</span> array[j] &gt; array[j+<span class="number">1</span>]:</span><br><span class="line">                array[j], array[j+<span class="number">1</span>] = array[j+<span class="number">1</span>], array[j]</span><br><span class="line">    print(array)</span><br><span class="line">    <span class="keyword">return</span> array</span><br></pre></td></tr></table></figure><blockquote><p>建议自己画图或者Debug调试看看排序过程中元素的变化，更好去理解。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 算法 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> python </tag>
            
            <tag> 排序 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>python实现二分查找法</title>
      <link href="/binary-search.html"/>
      <url>/binary-search.html</url>
      
        <content type="html"><![CDATA[<p>二分查找也称折半查找（Binary Search），它是一种效率较高的查找方法。<br>但是，折半查找要求线性表必须采用顺序存储结构，而且表中元素按关键字有序排列</p><p><strong>注意</strong>：必须是从有序列表中找。</p><p>看看下面的例子，从列表[1, 2, 3, 4, 5, 6, 7, 8, 9]中找到数字”2”所在的位置。</p><p>使用二分查找法找到数字”2”的过程图解</p><p><img src="https://i.loli.net/2019/06/26/5d12e34ab9cc871563.png" alt="二分查找"></p><a id="more"></a><p>通过与列表中间的数做比较，判断需要寻找的数在列表的哪一部分，直到找到该元素的位置，或者该数字不存在于列表中。</p><p>循环条件就是两个边界位置的索引，小的<strong>小于等于</strong>大的。看看有哪些跳出循环的条件：</p><ol><li><p>要寻找的数大于最大值或者小于最小值。</p></li><li><p>找到需要查找的数</p></li><li><p>循环结束</p></li></ol><p>下面看看<a href="https://github.com/ZhangzhiS/study_note/blob/master/binary_search/binary_search.py" target="_blank" rel="noopener">python代码</a>是怎么实现的吧：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">binary_search</span><span class="params">(target: list, num: int)</span> -&gt; int or <span class="keyword">None</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    二分查找</span></span><br><span class="line"><span class="string">    :param target: 有序数组</span></span><br><span class="line"><span class="string">    :param num: 要查找的数字</span></span><br><span class="line"><span class="string">    :return: 要查找的数字所在的位置或者None</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    low = <span class="number">0</span>  <span class="comment"># 要查找的列表的左边边界索引位置</span></span><br><span class="line">    high = len(target) - <span class="number">1</span>  <span class="comment"># 要查找的列表的右边边界索引位置</span></span><br><span class="line">    <span class="keyword">while</span> low &lt;= high:</span><br><span class="line">        <span class="keyword">if</span> num &lt; target[low] <span class="keyword">or</span> num &gt; target[high]:</span><br><span class="line">            <span class="comment"># 如果要查找的数的小于最小值或者大于最大值，则结束</span></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line">        <span class="comment"># 找到位于列表中间的元素的索引位置</span></span><br><span class="line">        mid = int((low + high) / <span class="number">2</span>)</span><br><span class="line">        <span class="comment"># 该元素的值</span></span><br><span class="line">        guess = target[mid]</span><br><span class="line">        <span class="keyword">if</span> guess == num:</span><br><span class="line">            <span class="keyword">return</span> mid</span><br><span class="line">        <span class="keyword">elif</span> guess &gt; num:</span><br><span class="line">            high = mid - <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            low = mid + <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">None</span></span><br></pre></td></tr></table></figure><blockquote><p>建议自己画图或者Debug调试看看查找过程。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 算法 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> python，查找 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>leetcode-python-合并两个有序数组</title>
      <link href="/add-two-numbers.html"/>
      <url>/add-two-numbers.html</url>
      
        <content type="html"><![CDATA[<h2 id="两数相加"><a href="#两数相加" class="headerlink" title="两数相加"></a><a href="https://leetcode.com/problems/add-two-numbers/" target="_blank" rel="noopener">两数相加</a></h2><p>题目的中文地址：<a href="https://leetcode-cn.com/problems/add-two-numbers/" target="_blank" rel="noopener">两数相加</a></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 给出两个*非空*的链表用来表示两个*非负*的整数。</span></span><br><span class="line"><span class="comment"># 其中，它们各自的位数是按照**逆序**的方式存储的，并且它们的每个节点只能存储一位数字。</span></span><br><span class="line"><span class="comment"># 如果，我们将这两个数相加起来，则会返回一个新的链表来表示它们的和。</span></span><br><span class="line"><span class="comment"># 您可以假设除了数字 0 之外，这两个数都不会以 0 开头。</span></span><br><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="comment"># 输入：(2 -&gt; 4 -&gt; 3) + (5 -&gt; 6 -&gt; 4)</span></span><br><span class="line"><span class="comment"># 输出：7 -&gt; 0 -&gt; 8</span></span><br><span class="line"><span class="comment"># 原因：342 + 465 = 807</span></span><br></pre></td></tr></table></figure><a id="more"></a><h2 id="我的思路"><a href="#我的思路" class="headerlink" title="我的思路"></a>我的思路</h2><ul><li>Runtime: <strong>84 ms</strong>, faster than 47.11% of Python3 online submissions for Add Two<br>Numbers.</li><li>Memory Usage: <strong>13.3 MB</strong>, less than 33.94% of Python3 online submissions for Add Two Numbers.</li></ul><p>方法比较傻，分了三步，代码就不贴在这来，有兴趣可以看看<a href="https://github.com/ZhangzhiS/study_note/blob/master/add_two_numbers/add_two_numbers.py" target="_blank" rel="noopener">add_two_numbers.py</a></p><ol><li><p>将两个链表转为整数</p></li><li><p>两数相加</p></li><li><p>将结果转为链表</p></li></ol><p>提交的代码虽然执行完成了，但是占用的空间以及使用的时间比较长，属于leetcode提交结果中排名末尾的。</p><h2 id="别人家的思路"><a href="#别人家的思路" class="headerlink" title="别人家的思路"></a>别人家的思路</h2><p>执行时间：<strong>sample 48 ms submission</strong></p><p>用时<em>48ms</em>，虽然和我的84ms有点像😯。</p><p>看看代码</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Definition for singly-linked list.</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ListNode</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, x)</span>:</span></span><br><span class="line">        self.val = x</span><br><span class="line">        self.next = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">addTwoNumbers</span><span class="params">(self, l1: ListNode, l2: ListNode)</span> -&gt; ListNode:</span></span><br><span class="line"></span><br><span class="line">        temp = <span class="number">0</span></span><br><span class="line">        node = l1</span><br><span class="line">        root = node</span><br><span class="line">        <span class="keyword">while</span> l1 <span class="keyword">or</span> l2:</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> l1:</span><br><span class="line">                temp = l2.val + temp // <span class="number">10</span></span><br><span class="line">                l2 = l2.next</span><br><span class="line">            <span class="keyword">elif</span> <span class="keyword">not</span> l2:</span><br><span class="line">                temp = l1.val + temp // <span class="number">10</span></span><br><span class="line">                l1 = l1.next</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                temp = l1.val + l2.val + temp // <span class="number">10</span></span><br><span class="line">                l1 = l1.next</span><br><span class="line">                l2 = l2.next</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> node.next <span class="keyword">and</span> l2:</span><br><span class="line">                node.next = ListNode(<span class="number">0</span>)</span><br><span class="line">            node.val = temp % <span class="number">10</span></span><br><span class="line">            <span class="keyword">if</span> node.next:</span><br><span class="line">                node = node.next</span><br><span class="line">        <span class="keyword">if</span> temp &gt;= <span class="number">10</span>:</span><br><span class="line">            node.next =  ListNode(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> root</span><br></pre></td></tr></table></figure><p>将两个链表对应的节点的值相加，每次都将相加的结果保存，在计算下个节点的时候，判断是否满10进1。</p><p>在程序中，root在内存地址中是等于l1的，所以其实是在链表l1上进行原地操作，会减少程序占用的空间。</p><p>用例子中的<code>(2 -&gt; 4 -&gt; 3) + (5 -&gt; 6 -&gt; 4)</code>来看看怎么执行的。</p><p><strong>注意</strong>：在图片中的l1和l2是表示在函数执行过程中节点指向的变化，不是直真实的传入参数l1、l2两个参数的变化，这里需要了解Python里面深复制，浅复制，可变与不可变对象的区别。</p><p>初始状态</p><p><img src="https://i.loli.net/2019/06/20/5d0b19276a61455852.png" alt="start"></p><p>第一次循环</p><p><img src="https://i.loli.net/2019/06/20/5d0b197fd26f094779.png" alt="1"></p><p>第二次循环</p><p><img src="https://i.loli.net/2019/06/20/5d0b19ac7db1e73686.png" alt="2"></p><p>第三次循环</p><p><img src="https://i.loli.net/2019/06/20/5d0b19dbb15c552165.png" alt="3"></p><p>然后返回root，这里的root指向的其实是原来的l1。这个例子没有用到temp&gt;10<br>的判断，这个是为了在最后一个节点，最大位数进行相加结果大于十，需要进1的时候准备的，比如：</p><p>366+366</p><p>可以自己进行调试，看看执行的过程。</p>]]></content>
      
      
      <categories>
          
          <category> 算法 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> leetcode </tag>
            
            <tag> python </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>将selenium获得的cookies传递给requests.session().cookies</title>
      <link href="/share-cookies.html"/>
      <url>/share-cookies.html</url>
      
        <content type="html"><![CDATA[<p>写爬虫的过程中进行模拟登录的时候，有的网站会有滑块验证，不是很好处理，想到了使用selenium配合requests进行半自动登录，将selenium获取到的cookies共享给requests，然后使用requests进行后续的操作。</p><p>这里需要注意的就是，从selenium中获取的cookies是由字典组成的列表,</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">cookies = [</span><br><span class="line">    &#123;<span class="string">"name"</span>: <span class="string">"user"</span>, <span class="string">"value"</span>: <span class="string">"username"</span>&#125;,</span><br><span class="line">    &#123;<span class="string">"name"</span>: <span class="string">"pwd"</span>, <span class="string">"value"</span>: <span class="string">"password"</span>&#125;,</span><br><span class="line">    &#123;<span class="string">"name"</span>: <span class="string">"date"</span>, <span class="string">"value"</span>: <span class="string">"2019-06-18"</span>&#125;,</span><br><span class="line">    <span class="comment"># ...</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>但是通过阅读源码，发现session中的cookies是<code>RequestsCookieJar()</code>类，然后该类提供了<code>set()</code>方法进行插入。</p><a id="more"></a><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">RequestsCookieJar().set(name, value)</span><br></pre></td></tr></table></figure><p>下面看一个简单的例子：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">import requests</span><br><span class="line">from selenium import webdriver</span><br><span class="line"></span><br><span class="line">brower = webdriver.Chrome()</span><br><span class="line"><span class="comment"># 打开网站</span></span><br><span class="line">brower.get(<span class="string">"https://zz/zzs7.top"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取cookies</span></span><br><span class="line">selenium_cookies = brower.get_cookies()</span><br><span class="line"></span><br><span class="line">sess = requests.session()</span><br><span class="line"></span><br><span class="line">tmp_cookies = requests.cookies.RequestCookieJar()</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> item <span class="keyword">in</span> selenium_cookies:</span><br><span class="line">    tmp_cookies.set(item[<span class="string">"name"</span>], item[<span class="string">"value"</span>])</span><br><span class="line"></span><br><span class="line">sess.cookies.update(tmp_cookies)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 这时候使用的就是同一个cookies了。</span></span><br><span class="line">sess.get(<span class="string">"https://zz/zzs7.top"</span>)</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> selenium </tag>
            
            <tag> requests </tag>
            
            <tag> 爬虫 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>了解TOX以及简单使用</title>
      <link href="/tox.html"/>
      <url>/tox.html</url>
      
        <content type="html"><![CDATA[<p>本文是我学习知识的一个记录，只是简单了解使用TOX，，英文阅读能力不错的建议看看<a href="https://tox.readthedocs.io/en/latest/" target="_blank" rel="noopener">官方文档</a>进行深入的学习。</p><h2 id="什么是TOX"><a href="#什么是TOX" class="headerlink" title="什么是TOX"></a>什么是TOX</h2><p>简单来说，TOX是一个虚拟环境管理和命令行测试工具，目标是为了自动化，标准化的进行Python程序的测试。作用如下：</p><ul><li><p>使用不同的Python版本和解释器检查项目的包安装是否正确。</p></li><li><p>在每个环境中执行项目的测试工具，比如pytest等。</p></li><li><p>处于CI（持续集成）之前，能大幅减少测试工具所需要的时间。</p></li></ul><a id="more"></a><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>支持目前主流的python版本，以及其他解释器，如： CPython 2.7 and 3.4 or later, Jython-2.5.1, pypy-1.9ff</p><p>TOX可以直接安装在虚拟环境中。</p><ul><li>可以使用pip直接安装(推荐)</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pip install tox</span><br></pre></td></tr></table></figure><ul><li>使用源码安装</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> py_packages <span class="comment"># 是你需要将包源码存放的位置，建议和其他使用源码安装的包放在一起统一管理</span></span><br><span class="line">git <span class="built_in">clone</span> https://github.com/tox-dev/tox</span><br><span class="line"><span class="built_in">cd</span> tox</span><br><span class="line">pip install .</span><br><span class="line">tox --version <span class="comment"># 安装成功之后可以查看安装的版本</span></span><br></pre></td></tr></table></figure><h2 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h2><p>常用的是<code>tox.ini</code>格式的配置文件。下面简单介绍一下配置文件：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[tox]</span></span><br><span class="line"><span class="comment"># tox部分是全局配置</span></span><br><span class="line"><span class="attr">minversion</span> = <span class="number">3.4</span>.<span class="number">0</span>  <span class="comment"># 定义运行所需的tox的最小版本</span></span><br><span class="line"><span class="attr">envlist</span> = py27,py36  <span class="comment"># 需要测试的python环境，会按顺序测试，使用","隔开，下面会说如何定义环境</span></span><br><span class="line"><span class="attr">skipsdist</span> = <span class="literal">true</span>  <span class="comment"># tox默认会使用sdist构建包，对于测试来说没有必要，而且构建还会要求存在README、setup.py等文件，并且保证setup.py的格式符合要求等，所以跳过此步</span></span><br><span class="line"></span><br><span class="line"><span class="section">[testenv:py27]</span></span><br><span class="line"><span class="attr">commands</span> = python -c <span class="string">'print "Python2 out! Enjoy Python3 :D!"'</span></span><br><span class="line"></span><br><span class="line"><span class="section">[testenv:py36]</span></span><br><span class="line"><span class="attr">deps</span> =</span><br><span class="line">    -rrequirements.txt</span><br><span class="line"><span class="attr">commands</span> =</span><br><span class="line">    pytest</span><br><span class="line"></span><br><span class="line"><span class="comment"># 如上面的两个"testenv"，:后面的为测试环境的名字</span></span><br><span class="line"><span class="comment"># deps 是安装依赖的命令</span></span><br><span class="line"><span class="comment"># commands 是环境配好之后需要执行的命令</span></span><br></pre></td></tr></table></figure><h2 id="执行"><a href="#执行" class="headerlink" title="执行"></a>执行</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tox</span><br></pre></td></tr></table></figure><p>或者也可以在命令行中指定测试的环境</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tox -e py36</span><br></pre></td></tr></table></figure><h2 id="举个例子"><a href="#举个例子" class="headerlink" title="举个例子"></a>举个例子</h2><p>写一个简单的例子试试</p><p>app.py</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/python3.6</span></span><br><span class="line"><span class="comment"># -*- coding: UTF-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo_1</span><span class="params">(a, b)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> a+b</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo_2</span><span class="params">(a, b)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">f"两个参数分别是<span class="subst">&#123;a&#125;</span>,<span class="subst">&#123;b&#125;</span>"</span></span><br></pre></td></tr></table></figure><p>test.py</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/python3.6</span></span><br><span class="line"><span class="comment"># -*- coding: UTF-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> app <span class="keyword">import</span> foo_1, foo_2</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test_foo_1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">assert</span> foo_1(<span class="number">1</span>, <span class="number">2</span>) == <span class="number">3</span></span><br><span class="line">    <span class="keyword">assert</span> foo_1(<span class="string">"1"</span>, <span class="string">"2"</span>) == <span class="string">"12"</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test_foo_2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">assert</span> foo_2(<span class="number">1</span>, <span class="number">2</span>) == <span class="string">"两个参数分别是1,2"</span></span><br></pre></td></tr></table></figure><p>tox.ini</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[tox]</span></span><br><span class="line"><span class="attr">skipsdist</span> = <span class="literal">True</span></span><br><span class="line"><span class="attr">envlist</span> = py36</span><br><span class="line"></span><br><span class="line"><span class="section">[testenv:py36]</span></span><br><span class="line"><span class="attr">deps</span> =</span><br><span class="line">    -rrequirements.txt</span><br><span class="line"><span class="attr">commands</span> =</span><br><span class="line">    pytest test.py</span><br><span class="line"></span><br><span class="line"><span class="section">[pytest]</span></span><br><span class="line"><span class="attr">addopts</span> = -p <span class="literal">no</span>:warnings  <span class="comment"># 屏蔽测试中的WARNING警告</span></span><br></pre></td></tr></table></figure><p>看看执行结果</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">~/PycharmProjects/<span class="built_in">local</span> » tox                                                                                                                                                                       zhi@bogon</span><br><span class="line">py36 recreate: /Users/zhi/PycharmProjects/<span class="built_in">local</span>/.tox/py36</span><br><span class="line">py36 installdeps: -rrequirements.txt</span><br><span class="line">py36 installed: atomicwrites==1.3.0,attrs==19.1.0,importlib-metadata==0.17,more-itertools==7.0.0,packaging==19.0,pluggy==0.12.0,py==1.8.0,pyparsing==2.4.0,pytest==4.6.2,six==1.12.0,wcwidth==0.1.7,zipp==0.5.1</span><br><span class="line">py36 run-test-pre: PYTHONHASHSEED=<span class="string">'546851202'</span></span><br><span class="line">py36 run-test: commands[0] | pytest test.py</span><br><span class="line">============================================================================================ <span class="built_in">test</span> session starts =============================================================================================</span><br><span class="line">platform darwin -- Python 3.6.8, pytest-4.6.2, py-1.8.0, pluggy-0.12.0</span><br><span class="line">cachedir: .tox/py36/.pytest_cache</span><br><span class="line">rootdir: /Users/zhi/PycharmProjects/<span class="built_in">local</span>, inifile: tox.ini</span><br><span class="line">collected 2 items</span><br><span class="line"></span><br><span class="line"><span class="comment"># 这是测试进度，后面的"."是测试结果，如果出错，是显示的是F</span></span><br><span class="line">test.py ..                                                                                                                                                                                             [100%]</span><br><span class="line"></span><br><span class="line">========================================================================================== 2 passed <span class="keyword">in</span> 0.02 seconds ==========================================================================================</span><br><span class="line">__________________________________________________________________________________________________ summary ___________________________________________________________________________________________________</span><br><span class="line">  py36: commands succeeded</span><br><span class="line">  congratulations :)</span><br></pre></td></tr></table></figure><p><strong>注意：requirements.txt文件没有的话，可以不写，但是会有WARNING警告</strong>。</p>]]></content>
      
      
      <categories>
          
          <category> 知识点 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 测试 </tag>
            
            <tag> tox </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>GitHub的Pull-Request流程</title>
      <link href="/git-pull-request.html"/>
      <url>/git-pull-request.html</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>Pull-Request是<a href="https://github.com" target="_blank" rel="noopener">GitHub</a>上对开源项目贡献代码的工作流程。</p><h2 id="1-Fork原项目"><a href="#1-Fork原项目" class="headerlink" title="1. Fork原项目"></a>1. Fork原项目</h2><p><img src="https://help.github.com/assets/images/help/repository/fork_button.jpg" alt="Fork"></p><p>如上图，在项目的右上角，可以看到<code>Watch</code>、<code>Star</code>、<code>Fork</code>，三个按钮，点击<code>Fork</code>就可以了，成功之后在个人的仓库列表中可以看到你Fork的项目。</p><h2 id="2-将项目clone到本地"><a href="#2-将项目clone到本地" class="headerlink" title="2. 将项目clone到本地"></a>2. 将项目clone到本地</h2><p>在克隆项目的时候，克隆的是自己仓库的代码，而不是源项目的。比如你fork了user-A的项目<code>project-a</code>，源项目地址为<code>https://github.com/user-A/project-a</code>，在你仓库中的项目地址为<code>https://github.com/your-username/project-a</code>。</p><a id="more"></a><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/your-username/project<span class="_">-a</span></span><br></pre></td></tr></table></figure><h2 id="3-创建自己的分支"><a href="#3-创建自己的分支" class="headerlink" title="3. 创建自己的分支"></a>3. 创建自己的分支</h2><p>新建一个自己的分支，后续开发在自己分支中开发，创建分支的时候，在分支名字中表达出这个分支是进行BUG修复，或者是开发新功能。具体参考源项目的命名规范。注意创建分支的时候选择的上游分支，看项目发布规则，是在dev分支开发或者是master分支进行开发。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git branch fixbug_dosomethings [上游分支]</span><br></pre></td></tr></table></figure><h2 id="4-在自己的分支上进行开发"><a href="#4-在自己的分支上进行开发" class="headerlink" title="4. 在自己的分支上进行开发"></a>4. 在自己的分支上进行开发</h2><p>开发过程中的话，需要注意的就是代码规范了，最好是延续源项目的风格，当然，规范的开发风格都一样。经常提交代码是一个好习惯，避免异常状况发生之后造成损失。有权限的人可以看到你每次的提交</p><h2 id="5-发送一个Pull-Request"><a href="#5-发送一个Pull-Request" class="headerlink" title="5. 发送一个Pull-Request"></a>5. 发送一个Pull-Request</h2><p>在源项目新建一个Pull-Request，就在选择分支按钮的旁边，<code>new pull request</code>，base repository，选择源项目，head repository选择你自己的仓库，选择好对应的分支就可以提交。</p><h2 id="6-后续"><a href="#6-后续" class="headerlink" title="6. 后续"></a>6. 后续</h2><p>有权限的项目参与者能够看到你创建的合并，进行code review，或者进行测试，当他们认为可以合并，那么接受本次提交并合并到源项目的目标分支中。</p><p><strong>注意：本次提交没有关闭，你后续所有在这个分支上进行的修改，commit，都会进入此次pull-request中</strong>。</p>]]></content>
      
      
      <categories>
          
          <category> 杂项 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Git的简单使用</title>
      <link href="/use-git-1.html"/>
      <url>/use-git-1.html</url>
      
        <content type="html"><![CDATA[<h2 id="使用Git进行代码管理"><a href="#使用Git进行代码管理" class="headerlink" title="使用Git进行代码管理"></a>使用Git进行代码管理</h2><h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>在开发过程往往是多人协作开发，就需要一种合适的方式进行代码管理，进行代码版本管理。我接触到的两家公司都是使用的Git进行代码管理，工作过程遇到过不少问题，简单整理记录一下。</p><p>关于Git的底层实现就不详细说明了，如果想了解这方面的知识，可以去看看<a href="https://git-scm.com/book/zh/v2" target="_blank" rel="noopener">Git-Book</a>以及<a href="https://jwiegley.github.io/git-from-the-bottom-up/" target="_blank" rel="noopener">Git from the Bottom Up</a>。我只是为了记录一些常用的操作，之后可能会详细说说底层实现的东西。</p><h3 id="简单的流程以及命令"><a href="#简单的流程以及命令" class="headerlink" title="简单的流程以及命令"></a>简单的流程以及命令</h3><h4 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h4><p>平时大家自己学习某个技术或者空闲的时候肯定会自己写一些简单的项目，代码保存在本地肯定，机器挂了的话，就完蛋了，最好的办法是托管在知名的代码托管平台，首先想到的肯定是GitHub，还有Gitlab，国内的coding，腾讯开发者平台，码云等。都支持使用git工具进行代码管理。</p><a id="more"></a><p>一般我们会使用配置ssh-key，使用ssh来连接自己的代码仓库，不然使用https还需要输入密码，那太麻烦了。</p><p>生成ssh密钥，使用下面的命令，会让你选择保存位置，输入密码。如果没有特殊需求，一路回车就行了。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen</span><br></pre></td></tr></table></figure><p>生成密钥之后会保存在<code>~/.ssh</code>目录下，id_rsa是私钥，id_rsa.pub是公钥，私钥保存在你自己电脑上，公钥配置在代码托管平台上，这样就可以使用ssh连接你的代码仓库。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 目录下会生成两个文件</span></span><br><span class="line">id_rsa      id_rsa.pub</span><br></pre></td></tr></table></figure><h4 id="在本地初始化一个仓库"><a href="#在本地初始化一个仓库" class="headerlink" title="在本地初始化一个仓库"></a>在本地初始化一个仓库</h4><p>在需要初始化的位文件夹内执行下面的命令</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git init  <span class="comment"># 初始化</span></span><br><span class="line"><span class="comment"># 如果是在你的项目内，比如路径下有你的a.py，b.py两个文件</span></span><br><span class="line">git add a.py b.py  <span class="comment"># 将需要上传到代码仓库的文件添加到暂存区，支持文件夹、单个或者多个文件。</span></span><br><span class="line">git commit -m <span class="string">"本次提交做了什么修改"</span>  <span class="comment">#对这次提交代码的描述</span></span><br></pre></td></tr></table></figure><h4 id="关联远程仓库，并且将代码上传"><a href="#关联远程仓库，并且将代码上传" class="headerlink" title="关联远程仓库，并且将代码上传"></a>关联远程仓库，并且将代码上传</h4><p>在代码托管平台创建一个项目，此时项目是空的，你需要将你的代码提交到仓库中。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 建立与远程仓库的联系</span></span><br><span class="line">git remote add git@github.com:ZhangzhiS/ZhangzhiS.github.io.git</span><br><span class="line"><span class="comment"># 上面命令后面的链接是我自己的一个博客仓库，你可以复制你自己的项目链接</span></span><br><span class="line"><span class="comment"># 使用git push将代码推送到远程仓库</span></span><br><span class="line">git push</span><br></pre></td></tr></table></figure><h4 id="更换电脑之后将代码拉取到本地"><a href="#更换电脑之后将代码拉取到本地" class="headerlink" title="更换电脑之后将代码拉取到本地"></a>更换电脑之后将代码拉取到本地</h4><p>比如电脑坏了，或者其他原因换电脑了，可以把远程仓库的代码拉取到本地，当然你要先配置好ssh-key。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 直接把代码clone下来就好了</span></span><br><span class="line">git <span class="built_in">clone</span> git@github.com:ZhangzhiS/ZhangzhiS.github.io.git</span><br></pre></td></tr></table></figure><h4 id="两台电脑一起修改的代码的时候"><a href="#两台电脑一起修改的代码的时候" class="headerlink" title="两台电脑一起修改的代码的时候"></a>两台电脑一起修改的代码的时候</h4><p>这时候就是简单的协作了。比如A电脑进行了代码文件<code>a.py</code>进行了修改</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git add a.py</span><br><span class="line">git commit -m 修改xxx</span><br><span class="line">git push</span><br></pre></td></tr></table></figure><p>B电脑上没有A电脑修改的这部分代码，这时候可以将代码库中的修改之后的代码拉取到本地。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git pull</span><br></pre></td></tr></table></figure><p>此时你就会发现，<code>a.py</code>已经是修改之后的了。</p><p>上面说的简单流程是自己一个人开发的时候可以用到的，每次都是pull，push的工作。因为只是自己一个在做修改，发生冲突的概率是比较小的，但是真正工作中，经常会有你和同事修改同一个文件，发生冲突的概率也会很大。</p><p>后面我会说说Git的一些别的操作。</p>]]></content>
      
      
      <categories>
          
          <category> 杂项 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>关于同源策略</title>
      <link href="/same-origin-policy.html"/>
      <url>/same-origin-policy.html</url>
      
        <content type="html"><![CDATA[<h2 id="同源策略"><a href="#同源策略" class="headerlink" title="同源策略"></a>同源策略</h2><blockquote><p>同源策略是一种约定，是由<a href="https://baike.baidu.com/item/Netscape/2778944" target="_blank" rel="noopener">Netscape</a>提出的著名安全策略。他是浏览器最核心、最基本的安全功能。</p></blockquote><h3 id="同源的定义"><a href="#同源的定义" class="headerlink" title="同源的定义"></a>同源的定义</h3><p>如果两个页面的协议、端口、主机都相同，则两个页面具有相同的源。</p><a id="more"></a><p>下面做一些说明：</p><ol><li><a href="https://zz.zzs7.top/mysql-optimization.html">https://zz.zzs7.top/mysql-optimization.html</a></li><li><a href="http://zz.zzs7.top/mysql-optimization.html">http://zz.zzs7.top/mysql-optimization.html</a></li><li><a href="https://zz.zzs7.top:8000/mysql-optimization.html">https://zz.zzs7.top:8000/mysql-optimization.html</a></li><li><a href="https://s7.zzs7.top/mysql-optimization.html" target="_blank" rel="noopener">https://s7.zzs7.top/mysql-optimization.html</a></li><li><a href="https://104.125.11.39/mysql-optimization.html" target="_blank" rel="noopener">https://104.125.11.39/mysql-optimization.html</a></li></ol><p>上面的链接中：</p><ul><li>1和2比较：不是同源，因为1用的https，2用的是http。属于协议不同。</li><li>1和3比较：不是同源，1未指定端口的时候，默认使用的是80,而3使用的是8000端口，属于端口不同。</li><li>1和4比较：不是同源，域名不同。</li><li>1和5比较：虽然1部署在了该ip地址对应的服务器，但是依然不是同源。</li></ul><p>在不同源的情况下1站点里面的js脚本采用ajax读取其他三个站点中的数据是会报错的。</p><p><strong>注：IE浏览器未将不同端口加入同源策略的组成部分。</strong></p><h3 id="跨域"><a href="#跨域" class="headerlink" title="跨域"></a>跨域</h3><p>受同源策略的影响，不是 同源下的脚本不能操作其他源下面的对象。想要操作其他源下的对象就需要跨域。</p><h4 id="为什么要跨域"><a href="#为什么要跨域" class="headerlink" title="为什么要跨域"></a>为什么要跨域</h4><p>比如某视频网站由于数据太多，分布在了不同的服务器上，所以需要突破同源策略，实现数据交互。</p><h4 id="跨域的实现"><a href="#跨域的实现" class="headerlink" title="跨域的实现"></a>跨域的实现</h4><p>有以下几种实现方式：</p><ul><li><p><strong>降域（document.domain）</strong>：同源策略认为域和子域属于不同的域。</p><p>比如<a href="https://zz.zzs7.top和https://s7.zzs7.top" target="_blank" rel="noopener">https://zz.zzs7.top和https://s7.zzs7.top</a>。虽然都属于zzs7.top域名的子域名，但是他们属于不同域。</p><p>想让上述两个网页之间通信，可以通过设置 <code>document.damain=&#39;zzs7.top&#39;</code>，来让浏览器认为他们在同样的域。两个页面都需设置。</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line">    document.domain = 'zzs7.top';</span><br><span class="line">    // 获取父窗口中变量</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p><strong>注：可以进行降域，不能升域。</strong></p></li><li><p><strong>通过jsonp跨域</strong>：不同源虽然不能读写，但可以引用js。我们可以让引用的js附带数据来调用我们预先定义的函数，而数据当作参数传入。</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line">   var script = document.createElement('script');</span><br><span class="line">   script.type = 'text/javascript';</span><br><span class="line">  </span><br><span class="line">   // 传参一个回调函数名给后端，方便后端返回时执行这个在前端定义的回调函数</span><br><span class="line">   script.src = 'http://zz.zzs7.top/login?user=admin&amp;callback=handleCallback';</span><br><span class="line">   document.head.appendChild(script);</span><br><span class="line">  </span><br><span class="line">   // 回调执行函数</span><br><span class="line">   function handleCallback(res) &#123;</span><br><span class="line">       alert(JSON.stringify(res));</span><br><span class="line">   &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></li><li><p><strong>CORS：跨域资源共享</strong>：一个W3C标准，它允许浏览器向跨源服务器发起<code>XMLHttpRequest请求</code>，和ajax同源的使用方法一致（区别在于对于跨域请求浏览器的请求会有附加的相关设置，用户看不到），分简单请求和非简单请求。</p></li><li><p>通过 <strong>location.hash</strong>：a欲与b跨域相互通信，通过中间页c来实现。 三个页面，不同域之间利用iframe的location.hash传值，相同域之间直接js访问来通信。</p></li><li><p><strong>window.name</strong>：window.name属性的独特之处：name值在不同的页面（甚至不同域名）加载后依旧存在，并且可以支持非常长的 name 值（2MB）。</p></li><li><p><strong>postMessage跨域</strong>：postMessage是HTML5 XMLHttpRequest Level 2中的API，且是为数不多可以跨域操作的window属性之一，它可用于解决以下方面的问题：</p><ul><li>页面和其打开的新窗口的数据传递</li><li>多窗口之间消息传递</li><li>页面与嵌套的iframe消息传递</li><li>上面三个场景的跨域数据传递</li></ul><p>用法：postMessage(data,origin)方法接受两个参数<br>data： html5规范支持任意基本类型或可复制的对象，但部分浏览器只支持字符串，所以传参时最好用JSON.stringify()序列化。<br>origin： 协议+主机+端口号，也可以设置为”*”，表示可以传递给任意窗口，如果要指定和当前窗口同源的话设置为”/“。</p></li></ul><p>还有一些比的，比如可以通过nginx代理实现跨域等，就不在文章中细说了，后续有机会详细实践一下每种方式。</p>]]></content>
      
      
      <categories>
          
          <category> 杂项 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> 前端 </tag>
            
            <tag> 知识点 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>MySQL优化整理</title>
      <link href="/mysql-optimization.html"/>
      <url>/mysql-optimization.html</url>
      
        <content type="html"><![CDATA[<p>昨天面试的时候，被问到有关于MySQL的优化方面的知识。回答了一些sql语句方面的优化，以及分表，对于MySQL数据库设计的方面回答也不是很完善，所以回来重新整理了一下这方面的知识。整理的比较片面，如果想要学习更深的东西，推荐阅读专业的书籍。</p><h3 id="尽量遵循数据库设计范式"><a href="#尽量遵循数据库设计范式" class="headerlink" title="尽量遵循数据库设计范式"></a>尽量遵循数据库设计范式</h3><p>遵循设计范式，是为了能够建立冗余小，结构合理的数据库。最常见的设计范式有三个：</p><a id="more"></a><ol><li><p>第一范式：原子性</p><p>原子性是需要遵循的最基本的范式了，就是需要保证每个字段是最小的不可拆分的。比如下面的用户表就是不符合原子性的，用户信息中的数据是可以继续拆分的：</p><table><thead><tr><th>id</th><th>用户名</th><th>用户信息</th></tr></thead><tbody><tr><td>1</td><td>张三</td><td>男，25岁</td></tr><tr><td>2</td><td>李四</td><td>男，30岁</td></tr></tbody></table><p>正确的设计应该是这样的：</p><table><thead><tr><th>id</th><th>用户名</th><th>性别</th><th>年龄</th></tr></thead><tbody><tr><td>1</td><td>张三</td><td>男</td><td>25</td></tr><tr><td>2</td><td>李四</td><td>男</td><td>30</td></tr></tbody></table><p>合理遵循第一范式，根据业务需求来确定合理的字段。</p></li><li><p>第二范式：唯一性</p><p>在一张表中，只说明一件事情，比如商品表，就只保存商品的信息，价格型号等，存在单一主键，非主键依赖于主键。</p><p>拿之前的图片平台项目来举例，先看看下面这种结构：</p><table><thead><tr><th>图集id</th><th>图集</th><th>图片id</th><th>图片</th><th></th></tr></thead><tbody><tr><td>1</td><td>童年</td><td>1</td><td>a.jpg</td><td></td></tr><tr><td>1</td><td>童年</td><td>2</td><td>b.jpg</td><td></td></tr><tr><td>2</td><td>风景</td><td>3</td><td>c.jpg</td><td></td></tr></tbody></table><p>一个图集内会有多张图片，这样设计明显是不太合理的，一个图集有多张图片的话，一些字段会有大量的重复，再看看下面的：</p><table><thead><tr><th>图集id</th><th>图集</th></tr></thead><tbody><tr><td>1</td><td>童年</td></tr><tr><td>2</td><td>风景</td></tr></tbody></table><table><thead><tr><th>图片id</th><th>图片</th><th>所属图集</th></tr></thead><tbody><tr><td>1</td><td>a.jpg</td><td>1</td></tr><tr><td>2</td><td>b.jpg</td><td>1</td></tr><tr><td>3</td><td>c.jpg</td><td>2</td></tr></tbody></table><p>图集拆分成一个表，图片为一个表，很大程度上减少了数据库的冗余。</p></li><li><p>第三范式：每列都依赖主键，不存在依赖传递。</p><p>如下面的例子：</p><p>用户表</p><table><thead><tr><th>用户id</th><th>用户名</th></tr></thead><tbody><tr><td>1</td><td>张三</td></tr></tbody></table><p>图片表</p><table><thead><tr><th>图片id</th><th>用户id</th></tr></thead><tbody><tr><td>1</td><td>1</td></tr><tr><td>2</td><td>1</td></tr><tr><td>3</td><td>1</td></tr><tr><td>4</td><td>1</td></tr></tbody></table><p>这样在查图片的时候可以根据用户id找到用户的信息。</p></li></ol><h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><h4 id="为什么要建立索引呢"><a href="#为什么要建立索引呢" class="headerlink" title="为什么要建立索引呢"></a>为什么要建立索引呢</h4><p>索引是为了能够快速查询到需要的数据。在MySQL中，索引的存储类型有两种”b-tree”和”HASH”。</p><p>举例来说：</p><p>我们的用户表有5W条数据，想要查询某个openid对应的用户信息，如果没有索引，那系统会遍历所有数据，直到找到对应的数据。如果建立了索引，可以查询索引的信息，快速的找到对应的数据。</p><h4 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h4><ul><li>优点：<ul><li>所有字段类型都可以建立索引。</li><li>极大的加快的查询速度。</li></ul></li><li>缺点：<ul><li>创建和维护索引都需要消耗时间，数据量越大，创建维护需要的时间也越久。</li><li>索引的存储也需要占存储空间。</li><li>每次进行数据库的增删改，索引也需要变化。</li></ul></li></ul><h4 id="使用原则"><a href="#使用原则" class="headerlink" title="使用原则"></a>使用原则</h4><ul><li>对经常更新的表避免创建过多的索引，比如用户表，一般情况下只需要对用户id创建索引就好了。</li><li>数据量小的表不一定需要创建索引，因为数据量少，就算遍历的速度都不见得会比查索引慢。</li></ul><h4 id="索引的分类"><a href="#索引的分类" class="headerlink" title="索引的分类"></a>索引的分类</h4><p>介绍一下常用的三种索引：</p><ul><li>普用索引：基本索引类型，没有什么限制，允许在定义索引的列中插入重复值和空值，纯粹为了查询数据更快一点。</li><li>唯一索引：创建索引的列的值必须是唯一的，但是允许空值。</li><li>主键索引：特殊的唯一索引，使用较多，不允许有空值。</li></ul><p>其他的比如：全文索引，组合索引，空间索引等我没用过，就先不介绍了。</p><h4 id="创建索引"><a href="#创建索引" class="headerlink" title="创建索引"></a>创建索引</h4><p>可以在创建表的时候就创建索引，这是创建索引的语句格式。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CREATE TABLE 表名[字段名 数据类型]  [UNIQUE|FULLTEXT|SPATIAL|...] [INDEX|KEY] [索引名字] (字段名[length]) 　　[ASC|DESC]</span><br></pre></td></tr></table></figure><p>我自己的话，主要编程语言为Python，项目经验大部分为Django，所以介绍一下python创建索引。</p><p>使用Django框架的话，可以在需要创建索引的字段中添加可选参数<code>db_index=True</code>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Article</span><span class="params">(models.Model)</span>:</span></span><br><span class="line">    <span class="comment"># 使用db_index=True对title建立索引</span></span><br><span class="line">    title = models.CharField(<span class="string">'标题'</span>, max_length=<span class="number">200</span>, db_index=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure><p>或者在class Meta中添加：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Article</span><span class="params">(models.Model)</span>:</span></span><br><span class="line">    <span class="string">"""文章模型"""</span></span><br><span class="line">    title = models.CharField(<span class="string">'标题'</span>, max_length=<span class="number">200</span>,)</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Meta</span>:</span></span><br><span class="line">        indexes = [</span><br><span class="line">            models.Index(fields=[<span class="string">'title'</span>]),</span><br><span class="line">        ]</span><br></pre></td></tr></table></figure><p>使用sqlalchemy的话，在对应的字段中添加可选的<code>index=True</code>.</p><p>当然，如果不使用ORM，创建索引的方法与SQL语句是相同的。</p><h3 id="查询过程中SQL的注意事项、"><a href="#查询过程中SQL的注意事项、" class="headerlink" title="查询过程中SQL的注意事项、"></a>查询过程中SQL的注意事项、</h3><p>主要避免使用不合适的查询语句，造成数据库放弃使用索引而搜索全表。</p><p>比如：</p><ul><li>避免在where语句中使用<code>！=</code>或者<code>&lt;&gt;</code>。</li><li>避免在where语句中对字段的值进行null判断。</li><li>避免使用<code>or</code>，使用<code>union all</code>来连接两条SQL。</li><li>避免使用<code>in</code>和<code>not in</code>，尽量使用<code>between</code>。</li></ul><h3 id="分表"><a href="#分表" class="headerlink" title="分表"></a>分表</h3><p>分为水平分表和垂直分表：</p><ul><li>水平分表：依据数据表的逻辑关系将同一个表中的数据按照某种条件拆分到多台数据库。比如根据用户年龄段或者地区进行拆分。</li><li>垂直分表：按照不同的表来且分到不同的数据库。</li></ul><h3 id="读写分离"><a href="#读写分离" class="headerlink" title="读写分离"></a>读写分离</h3><ul><li>应用服务器对master进行写的操作，同步数据，读取的时候读slave数据库。</li><li>通过MySQL-Proxy调度进行读写分离。</li></ul><h3 id="MySQL配置优化"><a href="#MySQL配置优化" class="headerlink" title="MySQL配置优化"></a>MySQL配置优化</h3><p>我自己了解的只有配置并发数，以及调整缓存大小。详细的配置文件可以看看这个帖子。</p><p><a href="https://www.cnblogs.com/zhshto/p/6653424.html" target="_blank" rel="noopener">MySQL配置及优化</a></p>]]></content>
      
      
      <categories>
          
          <category> 数据库 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 数据库 </tag>
            
            <tag> MySQL </tag>
            
            <tag> 优化 </tag>
            
            <tag> 面试 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>如何在ArchLinux系统中安装MongoDB</title>
      <link href="/archlinux-install-mongodb.html"/>
      <url>/archlinux-install-mongodb.html</url>
      
        <content type="html"><![CDATA[<p>由于工作需要在本地安装mongodb用作调试，在安装过程中遇到了问题，记录一下。</p><p>首先看看安装成功的mongodb吧：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">[zhi@zhi-pc bin]$ mongod --version</span><br><span class="line">db version v4.0.9</span><br><span class="line">git version: fc525e2d9b0e4bceff5c2201457e564362909765</span><br><span class="line">allocator: tcmalloc</span><br><span class="line">modules: none</span><br><span class="line">build environment:</span><br><span class="line">    distarch: x86_64</span><br><span class="line">    target_arch: x86_64</span><br></pre></td></tr></table></figure><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>我的系统是Manjaro，基于archlinux的系统。由于MongoDB修改了软件授权协议，所以在archlinux的官方源中已经删除了MongoDB，在archlinux的wiki中建议安装AUR中的mongodb-bin，或者自己编译（需要180G的空余的磁盘空间）。脑阔疼，我没有配置AUR的源，自己编译的话，我这老爷机谁知道要编译多久，所以最后我选择了使用<a href="https://www.mongodb.com/download-center/community" target="_blank" rel="noopener">MongoDB官网</a>提供的软件包。</p><p>下面是安装过程：</p><a id="more"></a><ul><li><p>下载：在官网下载，我下载的是<a href="https://www.mongodb.com/dr/fastdl.mongodb.org/linux/mongodb-linux-x86_64-4.0.9.tgz/download" target="_blank" rel="noopener">4.0.9的版本</a>。</p></li><li><p>解压：将下载的压缩包解压到你需要安装的位置。我解压在了<code>/home/zhi/tools</code>中，由于加压的文件夹带有版本号<code>mongodb-linux-x86_64-4.0.9</code>，为了方便我重命名为了<code>mongodb</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">$ <span class="built_in">pwd</span></span><br><span class="line"><span class="comment"># /home/zhi/tools</span></span><br><span class="line">$ ls</span><br><span class="line"><span class="comment"># mongodb  mongodb-linux-x86_64-4.0.9.tgz</span></span><br></pre></td></tr></table></figure></li><li><p>将下面的代码加入到系统环境变量<code>.bashrc</code>末尾，然后执行<code>source ~/.bashrc</code>让它生效：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 将对应的路径改为你解压的路径</span></span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:/home/zhi/tools/mongodb/bin</span><br></pre></td></tr></table></figure></li><li><p>此时执行<code>mongod</code>是不能正常启动的，查看控制台输出</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2019-05-15T12:59:30.324+0800 I STORAGE  [initandlisten] exception <span class="keyword">in</span> initAndListen: NonExistentPath: Data directory /data/db not found., terminating</span><br></pre></td></tr></table></figure><p>发现是因为默认的数据目录是不存在的，默认位置为/data。可以选择新建/data目录，然后启动，或者自定义data的位置，启动mongod的时候指定data路径。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 使用默认位置，可能需要管理员权限去新建，然后记得修改目录权限</span></span><br><span class="line">mkdir /data</span><br><span class="line">mongod <span class="comment"># 启动</span></span><br><span class="line"><span class="comment"># 与下面的二选一就好了</span></span><br><span class="line"><span class="comment"># 或者自定义data的位置</span></span><br><span class="line">mkdir /home/zhi/tools/mongodb/data</span><br><span class="line">mongod --dbpath /home/zhi/tools/mongodb/data <span class="comment"># 指定dbpath的启动</span></span><br></pre></td></tr></table></figure></li></ul><p>当然也可以选择配置AUR源来安装，但是我懒得配置，而且自己一般也不会用到AUR，所以就只介绍这一种方法了。</p><p>启动的话可以选择使用下面的命令来后台执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nohup mongod --dbpath /home/zhi/tools/mongodb/data &amp;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Linux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> mongodb </tag>
            
            <tag> Linux </tag>
            
            <tag> 数据库 </tag>
            
            <tag> Manjaro </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>leetcode-python-合并两个有序数组</title>
      <link href="/leetcode-merge-nums.html"/>
      <url>/leetcode-merge-nums.html</url>
      
        <content type="html"><![CDATA[<p>今天经历了人生第一次电话面试，有点紧张，所以自己感觉表现的不是很好，面试过程中面试官问到了两个算法题，都是leecode上面的题目，而且我都做过，但是当时一下子都想不起来，回答的方法也都是比较傻的方法，尤其是合并两个有序数组的题目，在上个月15日做过，但是今天面试没能好好回答。</p><p>以后做leetcode的算法题目的时候，需要将当时解题的思路也要记录，而不是只把题目上传就好。</p><p>先看看合并两个有序数组的题目：</p><blockquote><p>给定两个有序整数数组 <em>nums1</em> 和 <em>nums2</em>，将 <em>nums2</em> 合并到 <em>nums1</em> 中<em>，</em>使得 <em>num1</em> 成为一个有序数组。</p></blockquote><p>虽然可以将两个数组加起来，然后进行排序，但是这么做的话，就浪费了两个有序数组这个条件，可以通过归并排序的方式进行合并。</p><p>先说说这个题的解题思路以及流程，先看下面的两个列表：</p><p><img src="https://i.loli.net/2019/05/14/5cdadf780b67657259.png" alt="img"></p><a id="more"></a><p>如上图的两个列表，先创建一颗空列表m，接着对比指针i，j指向的两个元素的大小，将小的元素append到列表m中，然后把对应的指针加1，重复此步骤，直到某一个指针超出列表索引，将另一个列表中剩下的元素添加到m中。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>]</span><br><span class="line">b = [<span class="number">2</span>,<span class="number">4</span>,<span class="number">6</span>,<span class="number">8</span>,<span class="number">9</span>,<span class="number">10</span>]</span><br><span class="line">m = []</span><br><span class="line">i,j = <span class="number">0</span>,<span class="number">0</span></span><br><span class="line"></span><br><span class="line">a[i] &lt; b[j]  <span class="comment"># 1&lt;2</span></span><br><span class="line">m.append(a.pop(i))  <span class="comment"># m=[1]</span></span><br><span class="line"></span><br><span class="line">a[i] == b[j]  <span class="comment"># 2 == 2</span></span><br><span class="line">m.append(a[i])  <span class="comment"># m = [1, 2]</span></span><br><span class="line"></span><br><span class="line">a[i] &gt; b[j]  <span class="comment"># 3 &gt; 2</span></span><br><span class="line">m.append(b[j])  <span class="comment"># m = [1, 2, 2]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 重复上面的步骤直到某个指针大于索引，将另一个列表中剩下的元素添加到m中</span></span><br></pre></td></tr></table></figure><p>下面我们用代码来实现：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">merge_sort</span><span class="params">(nums1, nums2)</span>:</span></span><br><span class="line">    m = []</span><br><span class="line">    i, j = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">    l_1, l_2 = len(nums1)<span class="number">-1</span>, len(nums2)<span class="number">-1</span></span><br><span class="line">    <span class="comment"># 当i，j的索引位置小于等于索引最大值的时候</span></span><br><span class="line">    <span class="keyword">while</span> i &lt;= l_1 <span class="keyword">and</span> j &lt;= l_2:</span><br><span class="line">        <span class="keyword">if</span> nums1[i] &lt;= nums2[j]:</span><br><span class="line">            m.append(nums1[i])</span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            m.append(nums2[j])</span><br><span class="line">            j += <span class="number">1</span></span><br><span class="line">    m = m + nums1[i:] + nums2[j:]</span><br><span class="line">    <span class="keyword">return</span> m</span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    n1 = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]</span><br><span class="line">    n2 = [<span class="number">2</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>]</span><br><span class="line">    m = merge_sort(n1, n2)</span><br><span class="line">    print(m)</span><br><span class="line">    <span class="comment"># [1, 2, 2, 3, 4, 4, 5, 5, 6, 7]</span></span><br></pre></td></tr></table></figure><p>我们也可以使用Python中的列表方法<code>pop()</code>来进行，只比较每个数组的第一位就可以，每次将两者小的一个数弹出添加到新的列表中，直到有一个列表为空：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">merge_sort</span><span class="params">(nums1, nums2)</span>:</span></span><br><span class="line">    m = []</span><br><span class="line">    i, j = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> nums1 <span class="keyword">and</span> nums2:</span><br><span class="line">        <span class="keyword">if</span> nums1[i] &lt;= nums2[j]:</span><br><span class="line">            temp = nums1.pop(i)</span><br><span class="line">            m.append(temp)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            temp = nums2.pop(j)</span><br><span class="line">            m.append(temp)</span><br><span class="line"></span><br><span class="line">    m = m + nums1 + nums2</span><br><span class="line">    <span class="keyword">return</span> m</span><br></pre></td></tr></table></figure><p>这种方法其实和上面的思路一样，只是实现方式不同而已。</p>]]></content>
      
      
      <categories>
          
          <category> 算法 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> leetcode </tag>
            
            <tag> python </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Python的并发编程（七）- 如何规避GIL带来的限制</title>
      <link href="/python-concurrent-7.html"/>
      <url>/python-concurrent-7.html</url>
      
        <content type="html"><![CDATA[<p>我们已经听说过全局解释器锁（GIL），担心会影响到多线程的性能。尽管Python完全支持多线程编程，但是在解释器的C语言实现中，有一部分并不是线程安全的，因此不能完全支持并发执行。</p><p>事实上，解释器被一个称为全局解释器锁的东西保护着，在任意时刻只允许一个Python线程投入执行。GIL带来的最明显的影响就是多线程的python程序无法充分利用多个CPU核心带来的优势（即，一个采用多线程技术的计算密集型应用只能在一个CPU上运行）。</p><p>在讨论规避GIL的常用方案之前，需要重点强调的是，GIL只会对CPU密集型的程序产生影响（主要完成计算任务的程序）。如果我们的程序主要是在做I/O操作，比如处理网络连接，那么选择多线程技术常常是一个明智的选择。因为他们大部分时间都花在等待对放发起连接上了。实际上可以创建数以千计的Python线程都没问题。在现代的计算机上运行这么多线程是不会有问题的。</p><p>在理解这部分的内容的时候，你可以将存在全局解释器锁的Python解释器看作一个加油站。</p><p><img src="https://i.loli.net/2019/05/14/5cda794e88ce223620.png" alt="img"></p><a id="more"></a><p><strong>I/O密集型</strong>任务中，你可以理解为有多辆小轿车驶入了加油站进行加油，虽然不知道哪个先加完，但是会比只有一个加油点的加油站顺序很快。如果是<strong>计算密集型</strong>的任务呢，就是当这辆车驶入加油站的时候，加油站所有的资源都用来服务这一辆车了，别的车只能等待。</p><p>当然上述比喻只是为了大家方便理解两种不同类型任务的区别。</p><p>对于CPU密集型的程序，我们需要对问题的本质做些研究，例如，仔细选择底层使用的算法，这可能会比尝试将一个没有优化过的算法用多线程来并行处理所带来的性能提升要多得多。同样的，由于Python是解释型语言，往往只需要简单的将性能关键的代码转移到C语言扩展的模块中就可能得到极大的性能提升。类似NumPy这样的扩展模块对于加速涉及数组数据的特定计算也是非常高效的。最后但同样重要的是，还可以尝试使用其他的解释器实现，比如说使用了JIT编译优化技术的PYPY。</p><p>同样值得指出的是，使用多线程技术并不是为了获得性能的提升。一个CPU密集型的程序可能会用多线程来管理图形用户界面、网络连接或者其他类型的服务。在这种情况下GIL实际上会带来更多的问题。因为如果某部分代码持有GIL锁的时间过长，那就会导致其他非CPU密集型的线程都阻塞住。实际上，一个写的很糟糕的C扩展模块会让这个问题更加严重，尽管代码中C实现的部分会比之前运行的快。</p><p>说这么多，要规避GIL的限制主要有两种常用的策略。</p><ol><li><p>如果完全使用Python来编程，可以使用<code>multiprocessin</code>模块来创建进程池，把它当作协处理器来使用。看看下面的例子：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 大量的计算任务 (CPU bound)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">some_work</span><span class="params">(args)</span>:</span></span><br><span class="line">    <span class="comment"># ...</span></span><br><span class="line">    result = args</span><br><span class="line">    <span class="keyword">return</span> result</span><br><span class="line"></span><br><span class="line"><span class="comment"># 线程函数</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">some_thread</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="comment"># ...</span></span><br><span class="line">        args = <span class="literal">None</span></span><br><span class="line">        r = some_work(args)</span><br><span class="line">        <span class="comment"># ...</span></span><br></pre></td></tr></table></figure><p>上面是使用线程去处理这个任务，下面我们可以将代码改为进程池的方式：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">pool = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 大量的计算任务 (CPU bound)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">some_work</span><span class="params">(args)</span>:</span></span><br><span class="line">    <span class="comment"># ...</span></span><br><span class="line">    result = args</span><br><span class="line">    <span class="keyword">return</span> result</span><br><span class="line"></span><br><span class="line"><span class="comment"># 线程函数</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">some_thread</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="comment"># ...</span></span><br><span class="line">        r = pool.apply(some_work, (args))</span><br><span class="line">        <span class="comment"># ...</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    <span class="keyword">import</span> multiprocessing</span><br><span class="line">    pool = multiprocessing.Pool()</span><br></pre></td></tr></table></figure><p>使用进程池的例子通过一个巧妙的办法避开了GIL的限制。每当有线程要执行CPU密集型任务的时候，就把任务提交到进程池中，然后进程池将任务转交给运行在另一个进程中的python解释器。当线程等待结果的时候就会释放GIL。此外，由于计算是在另一个单独的解释器中进行的，这就不再受到GIL的限制了，在多核系统上，将会发现采用这种技术能够轻易利用到所有的CPU核心。</p></li><li><p>第二种方式是把重点放在C语言的扩展编程上。主要思想就是将计算密集的任务转移到C语言中，使其独立于Python，在C代码中释放GIL，这里由于我也不会C，就不做示例了。</p></li></ol><p>在我们面对多线程程序性能问题的时候，不能去抱怨GIL是所有问题的根源。但是，这么做只是一种短视和幼稚的行为。举个例子，在多线程网络程序中出现神秘的“僵死”现象，这种现象可能是别的原因造成的，和GIL没有一点关系。所以要先认真研究自己的代码，判断GIL是否才是问题的关键。<strong>CPU密集型的处理才是需要考虑GIL，I/O密集的处理则不必要考虑</strong>。</p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> 并发 </tag>
            
            <tag> 多进程 </tag>
            
            <tag> 进程池 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Python的并发编程（六）- 多进程</title>
      <link href="/python-concurrent-6.html"/>
      <url>/python-concurrent-6.html</url>
      
        <content type="html"><![CDATA[<p>之前学习了多线程以及线程池，他们在执行I/O密集的程序的时候，性能是很高的，但是如果我们有大量的CPU密集型工作的程序，现在想利用多个CPU的优势运行的更快，应该怎么解决呢？</p><p>这时候，就不能使用多线程了，而是需要真正的并行来解决问题。</p><p>在<code>concurrent.futures</code>库中提供了一个<code>ProcessPoolExecutor</code>类，可用来在单独运行的python解释器实例中执行计算密集的函数。</p><p><code>ProcessPoolExecutor</code>的典型用法是下面这样的：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> concurrent.futures <span class="keyword">import</span> ProcessPoolExecutor</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> ProcessPoolExecutor() <span class="keyword">as</span> pool:</span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    在进程池pool中并行执行任务</span></span><br><span class="line"><span class="string">    """</span></span><br></pre></td></tr></table></figure><a id="more"></a><p>在底层，<code>ProcessPoolExecutor</code>创建了N个独立运行的Python解释器，这里的N就是系统上检测到的可用的CPU个数。可以创建和修改Python的进程数，只要给<code>ProcessPoolExecutor(N)</code>提供一个可选的参数。进程池会一直运行，直到with语句块中的最后一条语句执行完毕为止，此时进程池就会关闭。但是程序会一直等待所有已经提交的任务都处理完毕为止。</p><p>提交到进程池中的任务必须定义为函数形式。有两种方法可以提交任务。如果想并行处理一个列表推导式或者map()操作，可以使用<code>pool.map()</code>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> concurrent.futures <span class="keyword">import</span> ProcessPoolExecutor</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work</span><span class="params">(x)</span>:</span></span><br><span class="line">    <span class="string">"""任务逻辑"""</span></span><br><span class="line">    <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line">data = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> ProcessPoolExecutor() <span class="keyword">as</span> pool:</span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    在进程池pool中并行执行任务</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    results = pool.map(work, data)</span><br></pre></td></tr></table></figure><p>另一种方式是通过<code>pool.submit()</code>来手动提交单独的任务：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> concurrent.futures <span class="keyword">import</span> ProcessPoolExecutor</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work</span><span class="params">(x)</span>:</span></span><br><span class="line">    <span class="string">"""任务逻辑"""</span></span><br><span class="line">    <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line">data = <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> ProcessPoolExecutor() <span class="keyword">as</span> pool:</span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    在进程池pool中并行执行任务</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    future_result = pool.submit(work, data)</span><br><span class="line">    result = future_result.result()</span><br></pre></td></tr></table></figure><p>如果手动提交任务，得到的结果就是一个Future实例。要获取到结果还需要手动调用<code>result()</code>方法。这么做会阻塞进程，直到结果返回为止。所以与其让进程阻塞，不如提供一个回调函数，让他执行任务完成时出发执行。示例如下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> concurrent.futures <span class="keyword">import</span> ProcessPoolExecutor</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work</span><span class="params">(x)</span>:</span></span><br><span class="line">    <span class="string">"""任务逻辑"""</span></span><br><span class="line">    <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">when_done</span><span class="params">(r)</span>:</span></span><br><span class="line">    print(<span class="string">"result："</span>, r.result())</span><br><span class="line"></span><br><span class="line">data = <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> ProcessPoolExecutor() <span class="keyword">as</span> pool:</span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    在进程池pool中并行执行任务</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    future_result = pool.submit(work, data)</span><br><span class="line">    future_result.add_done_callback(when_done)</span><br></pre></td></tr></table></figure><p>用户提供的回调函数需要接受一个Future实例，必须用他才能获取实际的结果。</p><p>尽管进程池看起来很简单，但是在设计规模更大的程序时有下面几个重要的因素需要考虑。</p><ul><li>这种并行处理的技术只适用于可以将问题分解成各个独立部分的情况。</li><li>任务必须定义成普通函数来提交。实例方法，比包或者其他类型的可调用对象都是不支持并行处理的。</li><li>函数的参数和返回值必须可兼容<code>pickle</code>编码。任务的执行是在单独的解释器进程中完成的。这中间需要进程间通信。因此，在不同的解释器之间交换数据必须要进行序列化处理。</li><li>提交的工作函数都不应该维护持久的状态或者带有副作用。除了简单的日志功能，一旦子进程启动，将无法控制他的行为。因此，为了让思路保持清晰，最好让每件事情都保持简单，让任务在不会修改执行环境的纯函数中执行。</li><li>进程池是通过调用UNIX上的fork()系统调用来创建的。这么做会克隆一个Python解释器，在fork()时会包含所有的程序状态。在windows上，这么做会加载一个独立的解释器拷贝，但不包含状态。克隆出来的进程在首次调用<code>pool.map()</code>或者<code>pool.submit()</code>之前不会实际运行。</li><li>既然是克隆一个独立的解释器，那每个进程都可以再执行线程。当进程池和多线程技术结合在一起使用的时候需要格外的小心。特别是，很可能我们应该在创建任何线程之前优先创建并加载进程池（例如，当程序启动时在主线程中创建进程池）。</li></ul>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> 并发 </tag>
            
            <tag> 多进程 </tag>
            
            <tag> 进程池 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Python的并发编程（五）- 线程池</title>
      <link href="/python-concurrent-5.html"/>
      <url>/python-concurrent-5.html</url>
      
        <content type="html"><![CDATA[<h2 id="Python的并发编程（五）-线程池"><a href="#Python的并发编程（五）-线程池" class="headerlink" title="Python的并发编程（五）- 线程池"></a>Python的并发编程（五）- 线程池</h2><p>之前的文章学习了一些多线程的用法，在I/O密集型的程序中，多线程带来了显著的性能提升，那我们可以无限制的，大量的创建多线程任务吗？</p><p>一个线程创建、销毁都是需要消耗系统资源的，如果线程数大于一定的数量，线程的创建销毁就会占用大量的系统性能，就不能充分利用到系统资源了。这时候，可以选择使用线程池了，</p><a id="more"></a><h3 id="线程池"><a href="#线程池" class="headerlink" title="线程池"></a>线程池</h3><p>创建一定数量的线程来执行任务，任务结束之后，该线程不进行销毁，而是继续从任务队列中获取新任务来执行，直到所有任务都执行结束之后，关闭所有线程。</p><p><code>concurrent.futures</code>库中包含有一个<code>ThreadPoolExecutor</code>类可用来实现这个目的。下面来实现一个简单的TCP服务器，使用线程池来服务客户端：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> AF_INET, socket, SOCK_STREAM</span><br><span class="line"><span class="keyword">from</span> concurrent.futures <span class="keyword">import</span> ThreadPoolExecutor</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">echo_client</span><span class="params">(sock, client_addr)</span>:</span></span><br><span class="line">    print(<span class="string">"&#123;addr&#125;已连接"</span>.format(addr=client_addr))</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        msg = sock.recv(<span class="number">40960</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> msg:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        sock.sendall(msg)</span><br><span class="line">    sock.close()</span><br><span class="line">    print(<span class="string">"关闭连接"</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">echo_server</span><span class="params">(addr)</span>:</span></span><br><span class="line">    pool = ThreadPoolExecutor(<span class="number">128</span>)</span><br><span class="line">    sock = socket(AF_INET, SOCK_STREAM)</span><br><span class="line">    sock.bind(addr)</span><br><span class="line">    sock.listen(<span class="number">5</span>)</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        client_sock, client_addr = sock.accept()</span><br><span class="line">        pool.submit(echo_client, client_sock, client_addr)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    echo_server((<span class="string">""</span>, <span class="number">5000</span>))</span><br></pre></td></tr></table></figure><p>当然，我们也可以手动创建线程池，使用<code>Queue</code>来作为任务队列。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> AF_INET, socket, SOCK_STREAM</span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"><span class="keyword">from</span> queue <span class="keyword">import</span> Queue</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">echo_client</span><span class="params">(q)</span>:</span></span><br><span class="line">    sock, client_addr = q.get()</span><br><span class="line">    print(<span class="string">"&#123;addr&#125;已连接"</span>.format(addr=client_addr))</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        msg = sock.recv(<span class="number">40960</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> msg:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        sock.sendall(msg)</span><br><span class="line">    sock.close()</span><br><span class="line">    print(<span class="string">"关闭连接"</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">echo_server</span><span class="params">(addr, nworkers)</span>:</span></span><br><span class="line">    q = Queue()</span><br><span class="line">    <span class="keyword">for</span> n <span class="keyword">in</span> range(nworkers):</span><br><span class="line">        <span class="comment"># 启动nworkers个线程</span></span><br><span class="line">        t = Thread(target=echo_client, args=(q, ))</span><br><span class="line">        t.daemon = <span class="literal">True</span></span><br><span class="line">        t.start()</span><br><span class="line">    sock = socket(AF_INET, SOCK_STREAM)</span><br><span class="line">    sock.bind(addr)</span><br><span class="line">    sock.listen(<span class="number">5</span>)</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        client_sock, client_addr = sock.accept()</span><br><span class="line">        q.put(client_sock, client_addr)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    echo_server((<span class="string">""</span>, <span class="number">5000</span>), <span class="number">10</span>)</span><br></pre></td></tr></table></figure><p>尽量使用<code>ThreadPoolExecutor</code>而不是手动实现线程池。这么做的优势在于使得任务的提交者能够更容易从调用函数中取得结果。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> urllib.request</span><br><span class="line"><span class="keyword">from</span> concurrent.futures <span class="keyword">import</span> ThreadPoolExecutor</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fetch_url</span><span class="params">(url)</span>:</span></span><br><span class="line">    u = urllib.request.urlopen(url)</span><br><span class="line">    data = u.read()</span><br><span class="line">    <span class="keyword">return</span> data</span><br><span class="line"></span><br><span class="line">pool = ThreadPoolExecutor(<span class="number">10</span>)</span><br><span class="line">a = pool.submit(fetch_url, <span class="string">"http://www.python.org"</span>)</span><br><span class="line">b = pool.submit(fetch_url, <span class="string">"http://www.pypy.org"</span>)</span><br><span class="line"></span><br><span class="line">x = a.result()</span><br><span class="line">y = b.result()</span><br></pre></td></tr></table></figure><p>示例中的结果对象（即a和b）负责处理所有需要完成的阻塞和同步任务，从工作者线程中取回数据。特别是，<code>a.result()</code>操作会阻塞，直到对应的函数已经由线程执行完幷返回了结果为止。</p><p><strong>注意</strong>：避免写允许线程无限增长的程序。如果在web服务中这么做了，无法阻止恶意用户对服务器发起拒绝服务攻击，从而导致服务器上创建了大量的线程，耗尽了系统资源而崩溃。通过预先初始化好的线程池，就可以小心的为所有能支持的并发总数设定一个上限。</p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> 多线程 </tag>
            
            <tag> 并发 </tag>
            
            <tag> 线程池 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Python的并发编程（四）- 多线程中的锁</title>
      <link href="/python-concurrent-4.html"/>
      <url>/python-concurrent-4.html</url>
      
        <content type="html"><![CDATA[<p>如何在多线程中安全的使用可变对象呢？比如给文件中写入内容的时候，我们希望当前线程写入完毕之后，其他线程才能够继续操作这个文件，避免文件内容错乱。</p><h3 id="Lock对象"><a href="#Lock对象" class="headerlink" title="Lock对象"></a>Lock对象</h3><p>可以利用threading库中的Lock对象来解决问题。</p><a id="more"></a><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SharedCounter</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, initial_value=<span class="number">0</span>)</span>:</span></span><br><span class="line">        self._value = initial_value</span><br><span class="line">        self._value_lock = threading.Lock()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">incr</span><span class="params">(self, delta=<span class="number">1</span>)</span>:</span></span><br><span class="line">        <span class="string">"""增加"""</span></span><br><span class="line">        <span class="keyword">with</span> self._value_lock:</span><br><span class="line">            self._value += delta</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">decr</span><span class="params">(self, delta=<span class="number">1</span>)</span>:</span></span><br><span class="line">        <span class="string">"""减少"""</span></span><br><span class="line">        <span class="keyword">with</span> self._value_lock:</span><br><span class="line">            self._value -= delta</span><br></pre></td></tr></table></figure><p>当线程中使用with 语句时，Lock对象可以确保产生互斥的行为–也就是说，统一时间只允许一个线程with语句代码块中的逻辑。with 语句会在执行缩进的代码块时获取到锁，当控制流离开缩进语句时释放这个锁。</p><p>线程的调度从本质上来说是非确定性的。正因为如此，在多线程程序中如果不用好锁就会使得数据被随机的破坏掉，以及产生我们称之为竟态条件（race condition）的奇怪行为。要避免这些问题，只要共享的可变状态需要被多个线程访问，那么就需要使用锁。</p><p>当然还有以下这种写法：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SharedCounter</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="comment"># ....</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">incr</span><span class="params">(self, delta=<span class="number">1</span>)</span>:</span></span><br><span class="line">        self._value_lock.acquire()</span><br><span class="line">        self._value += delta</span><br><span class="line">        self._value_lock.release()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># ....</span></span><br></pre></td></tr></table></figure><p>不过with不是更优雅吗？而且with也不容易出错————尤其是我们在持有锁的时候抛出了异常，我们可能会忘记调用<code>release()</code>，而with语句总是会确保释放锁。</p><h3 id="避免死锁"><a href="#避免死锁" class="headerlink" title="避免死锁"></a>避免死锁</h3><p>在多线程程序中，出现死锁的常见原因就是线程一次尝试获取了多个锁，列如有一个线程获取到第一个锁，但是他在尝试获取第二个锁的时候阻塞了，那么这个线程可能会阻塞住其他线程的执行，进而使得整个程序僵死。</p><p>避免出现死锁的的一种解决方案就是给程序的每个锁分配一个唯一的数字编号，并且在获取多个锁时按照编号的升序方式来获取。看看示例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"><span class="keyword">from</span> contextlib <span class="keyword">import</span> contextmanager</span><br><span class="line"></span><br><span class="line">_local = threading.local()</span><br><span class="line"></span><br><span class="line"><span class="meta">@contextmanager</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">acquire</span><span class="params">(locks)</span>:</span></span><br><span class="line">    locks = sorted(locks, key=<span class="keyword">lambda</span> x: id(x))</span><br><span class="line"></span><br><span class="line">    acquired = getattr(_local, <span class="string">"acquired"</span>, [])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> acquired <span class="keyword">and</span> max(id(lock) <span class="keyword">for</span> lock <span class="keyword">in</span> acquired) &gt; id(locks[<span class="number">0</span>]):</span><br><span class="line">        <span class="keyword">raise</span> RuntimeError(<span class="string">"Lock Order Violation"</span>)</span><br><span class="line"></span><br><span class="line">    acquired.extend(locks)</span><br><span class="line">    _local.acquired = acquired</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">for</span> lock <span class="keyword">in</span> locks:</span><br><span class="line">            lock.acquire()</span><br><span class="line">        <span class="keyword">yield</span></span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="keyword">for</span> lock <span class="keyword">in</span> reversed(locks):</span><br><span class="line">            lock.release()</span><br><span class="line">        <span class="keyword">del</span> acquired[-len(locks):]</span><br></pre></td></tr></table></figure><p>要使用这个上下文管理器，只要按照正常的方式来分配锁对象，但是当相同一个或者多个锁打交道时就是用<code>acquire()</code>这个函数。例如：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"></span><br><span class="line">x_lock = threading.Lock()</span><br><span class="line">y_lock = threading.Lock()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">thread_1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="keyword">with</span> acquire(x_lock, y_lock):</span><br><span class="line">            print(<span class="string">"Thread-1"</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">thread_2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="keyword">with</span> acquire(x_lock, y_lock):</span><br><span class="line">            print(<span class="string">"Thread-2"</span>)</span><br><span class="line"></span><br><span class="line">t1 = threading.Thread(target=thread_1)</span><br><span class="line">t1.daemon = <span class="literal">True</span></span><br><span class="line">t1.start()</span><br><span class="line"></span><br><span class="line">t2 = threading.Thread(target=thread_1)</span><br><span class="line">t2.daemon = <span class="literal">True</span></span><br><span class="line">t2.start()</span><br></pre></td></tr></table></figure><p>如果运行这个程序，就会发现这个程序运行的很好，永远不会死锁。</p><p>本文只是提供简单的讲解，编码的时候灵活运用。</p><p>明天我们来说说线程池。</p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> 多线程 </tag>
            
            <tag> 并发 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Python的并发编程（三）-线程间通信</title>
      <link href="/python-concurrent-3.html"/>
      <url>/python-concurrent-3.html</url>
      
        <content type="html"><![CDATA[<p>在写多线程程序的时候，可能会有需求需要我们在线程之间交换数据</p><p>我们如何在线程之间实现安全的通信或者交换数据呢？</p><h3 id="Queue队列"><a href="#Queue队列" class="headerlink" title="Queue队列"></a>Queue队列</h3><p>也许将数据从一个线程发往另一个线程最安全的做法就是使用queue模块中的Queue（队列）了。</p><a id="more"></a><p>简单的流程：</p><ol><li>创建Queue实例，<strong>Queue实例会被所有的线程共享。</strong></li><li>put()添加元素</li><li>get()获取元素</li></ol><p><img src="https://i.loli.net/2019/05/08/5cd25f740a855.jpg" alt="img"></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">from</span> queue <span class="keyword">import</span> Queue</span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">producer</span><span class="params">(out_q)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>):</span><br><span class="line">        out_q.put(i)</span><br><span class="line">        time.sleep(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">consumer</span><span class="params">(in_q)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        data = in_q.get()</span><br><span class="line">        print(data)</span><br><span class="line"></span><br><span class="line">q = Queue()</span><br><span class="line">t1 = Thread(target=producer, args=(q, ))</span><br><span class="line">t2 = Thread(target=consumer, args=(q, ))</span><br><span class="line">t1.start()</span><br><span class="line">t2.start()</span><br></pre></td></tr></table></figure><p>Queue实例已经拥有了所有需要的锁，所以他们可以安全的在任意多线程之间共享。</p><p>如何对生产者和消费这的关闭过程进行同步协调？</p><p>我们可以用一个特殊的终止值，当我们把它放入队列，就使消费者退出。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">from</span> queue <span class="keyword">import</span> Queue</span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line">_sentinel = object()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">producer</span><span class="params">(out_q)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>):</span><br><span class="line">        out_q.put(i)</span><br><span class="line">        time.sleep(<span class="number">2</span>)</span><br><span class="line">    out_q.put(_sentinel)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">consumer</span><span class="params">(in_q)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        data = in_q.get()</span><br><span class="line">        <span class="keyword">if</span> data <span class="keyword">is</span> _sentinel:</span><br><span class="line">            in_q.put(_sentinel)</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        print(data)</span><br><span class="line"></span><br><span class="line">q = Queue()</span><br><span class="line">t1 = Thread(target=producer, args=(q, ))</span><br><span class="line">t2 = Thread(target=consumer, args=(q, ))</span><br><span class="line">t1.start()</span><br><span class="line">t2.start()</span><br></pre></td></tr></table></figure><p>在示例中，，当一个消费者收到这个退出信号之后，会退出。将终止值放回队列是因为如果有多个消费者，这样可以使其他监听这个队列的其他消费者线程也能够接收到这个终止值。</p><p>尽管队列是线程之间通信 的最常见的机制，但是只要添加了所需要的锁和同步功能，就可以构建自己的线程安全结构，最常见的做法就是将你的数据结构和条件变量打包在一起。</p><p>下面我们构建一个线程安全的优先级队列。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> heapq</span><br><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PriorityQueue</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self._queue = []</span><br><span class="line">        self._count = <span class="number">0</span></span><br><span class="line">        self._cv = threading.Condition()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">put</span><span class="params">(self, item, priority)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> self._cv:</span><br><span class="line">            heapq.heappush(self._queue, (-priority, self._count, item))</span><br><span class="line">            self._count += <span class="number">1</span></span><br><span class="line">            self._cv.notify()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">with</span> self._cv:</span><br><span class="line">            <span class="keyword">while</span> len(self._queue) == <span class="number">0</span>:</span><br><span class="line">                self._cv.wait()</span><br><span class="line">            <span class="keyword">return</span> heapq.heappop(self._queue)[<span class="number">-1</span>]</span><br></pre></td></tr></table></figure><p>通过队列实现的线程之间通信是一种单向的且不确定的过程。一般来说，我们无法得知接收线程（消费者）何时会实际接收到消息并开始工作。但是，Queue对象提供了一些基本的事件完成功能。</p><p><code>q.join()</code>会等待队列中所有的信息被消费。</p><p>Queue对象的<code>put()</code>和<code>get()</code>都支持非阻塞和超时机制。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> queue</span><br><span class="line"></span><br><span class="line">q = queue.Queue()</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    data = q.get(block=<span class="literal">False</span>)</span><br><span class="line"><span class="keyword">except</span> queue.Empty:</span><br><span class="line">    <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    q.put(<span class="string">"item"</span>, block=<span class="literal">False</span>)</span><br><span class="line"><span class="keyword">except</span> queue.Full:</span><br><span class="line">    <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    data = q.get(timeout=<span class="number">1</span>)</span><br><span class="line"><span class="keyword">except</span> queue.Empty:</span><br><span class="line">    <span class="keyword">pass</span></span><br></pre></td></tr></table></figure><p>可以避免特定的队列操作上无限期的阻塞下去。用法比较灵活多变。</p><p>最后还有一些别的实用方法， 比如<code>q.qsize()</code>、<code>q.full()</code>、<code>q.empty()</code>，他们能够告诉你队列的当前大小和状态。但是，这些方法在多线程环境中是不可靠的。例如：对<code>q.empty()</code>的调用可能会告诉我们队列是空的，但是在完成这个调用的同时，另外的线程可能已经往队列中添加了一个元素。</p><p>所以，在编写代码的时候不要太过于依赖这些函数。</p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> 多线程 </tag>
            
            <tag> 并发 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Python的并发编程（二）-多线程的执行状态</title>
      <link href="/python-concurrent-2.html"/>
      <url>/python-concurrent-2.html</url>
      
        <content type="html"><![CDATA[<p><a href="https://zz.zzs7.top/python-concurrent-1.html">上一篇</a>文章我们写了一个简单的多线程，我们使用<code>start()</code>来启动这个线程，但是如果我们想知道它实际会在什么时候开始运行呢？</p><h2 id="获取线程状态"><a href="#获取线程状态" class="headerlink" title="获取线程状态"></a>获取线程状态</h2><h3 id="使用Event同步线程状态"><a href="#使用Event同步线程状态" class="headerlink" title="使用Event同步线程状态"></a>使用Event同步线程状态</h3><p>线程的核心特点就是它们能够以非确定性的方式独立执行。即，什么时候开始执行、何时被打断、何时恢复执行等状态完全由操作系统来调度管理，这是用户和程序员都无法确定的。如果程序中有其他线程需要判断某个线程是否执行到了某个点，根据这个来判断后续的操作，那就产生了线程同步的问题。</p><p>要解决这类问题，我们可以使用threading中的Event对象。</p><a id="more"></a><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread, Event</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">countdown</span><span class="params">(n, started_evt)</span>:</span></span><br><span class="line">    print(<span class="string">"开始"</span>)</span><br><span class="line">    started_evt.set()</span><br><span class="line">    <span class="keyword">while</span> n &gt; <span class="number">0</span>:</span><br><span class="line">        print(<span class="string">"n="</span>, n)</span><br><span class="line">        n-=<span class="number">1</span></span><br><span class="line">        time.sleep(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建一个Event对象</span></span><br><span class="line">started_evt = Event()</span><br><span class="line"></span><br><span class="line">print(<span class="string">"启动 countdown"</span>)</span><br><span class="line"></span><br><span class="line">t = Thread(target=countdown, args=(<span class="number">10</span>, started_evt))</span><br><span class="line">t.start()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 等待线程开始</span></span><br><span class="line"></span><br><span class="line">started_evt.wait()</span><br><span class="line">print(<span class="string">"countdown 正在执行"</span>)</span><br></pre></td></tr></table></figure><p><code>Event</code>对象和条件标记（sticky flag）类似，允许县城等待某个事件发生。初始状态时事件被设置为0。如果时间没有被设置而线程正在等待该事件，那么线程就会被阻塞（休眠状态），直到事件被设置为止。当有线程设置了这个事件时，这回唤醒所有正在等待该事件的线程（如果存在）。如果线程等待的事件已经设置了，那么线程会继续执行。</p><p>上面的代码执行结果如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">启动 countdown</span><br><span class="line">开始</span><br><span class="line">n= 10</span><br><span class="line">countdown 正在执行</span><br><span class="line">n= 9</span><br><span class="line">n= 8</span><br><span class="line">n= 7</span><br><span class="line">n= 6</span><br><span class="line">n= 5</span><br><span class="line">n= 4</span><br><span class="line">n= 3</span><br><span class="line">n= 2</span><br><span class="line">n= 1</span><br></pre></td></tr></table></figure><p>“countdown 正在执行”总是会在“启动 countdown”之后打印，这里使用了时间来同步线程，使主线程等待。</p><p>代码执行的顺序为：</p><ol><li>t.start() 启动线程。</li><li>顺序执行到<code>started_evt.wait()</code>的时候，会等待<code>started_evt.set()</code>的执行。</li></ol><p>如果注释了<code>started_evt.set()</code>就会在<code>started_evt.wait()</code>处阻塞，“countdown 正在执行”也不会被打印，可以自己多修改代码调试，观察打印情况。</p><h3 id="使用Condition通知事件代替Event"><a href="#使用Condition通知事件代替Event" class="headerlink" title="使用Condition通知事件代替Event"></a>使用Condition通知事件代替Event</h3><p>Event对象最好之用于一次性的事件，也就是说，我们创建一个事件，让线程等待时间被设置，然后一旦完成了设置，Event对象就被丢弃。尽管可以使用Event对象的<code>clean()</code>方法来清除事件，但是要安全地清楚事件并等待它被再次设置这个过程很难同步协调，可能会在成事件丢失，死锁或者其他问题（特别是，在设置完事件之后，我们无法保证发起的时间清除请求就一定会在线程再次等待该事件之前被执行）。</p><p>比如上面的代码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">started_evt.clear()</span><br><span class="line">print(<span class="string">"tag"</span>)</span><br><span class="line">started_evt.wait()</span><br><span class="line">print(<span class="string">"countdown 正在执行11"</span>)</span><br></pre></td></tr></table></figure><p>在后面填家上面的代码，每次运行结果中，tag的打印位置是不确定的。</p><p>如果线程打算一遍又一遍地重复通知某个事件，那最好使用Condition对象来处理。</p><p>下面的代码实现了一个周期性的定时器，每当定时器超时，其他的线程都可感知到超时事件。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread, Condition</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PeriodicTimer</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, interval)</span>:</span></span><br><span class="line">        self._interval = interval</span><br><span class="line">        self._flag = <span class="number">0</span></span><br><span class="line">        self._cv = Condition()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">start</span><span class="params">(self)</span>:</span></span><br><span class="line">        t = Thread(target=self.run)</span><br><span class="line">        t.daemon = <span class="literal">True</span></span><br><span class="line">        t.start()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            time.sleep(self._interval)</span><br><span class="line">            <span class="keyword">with</span> self._cv:</span><br><span class="line">                self._flag ^= <span class="number">1</span></span><br><span class="line">                self._cv.notify_all()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wait_for_tick</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> self._cv:</span><br><span class="line">            last_flag = self._flag</span><br><span class="line">            <span class="keyword">while</span> last_flag == self._flag:</span><br><span class="line">                self._cv.wait()</span><br><span class="line"></span><br><span class="line">ptimer = PeriodicTimer(<span class="number">5</span>)</span><br><span class="line">ptimer.start()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">countdown</span><span class="params">(nticks)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> nticks &gt; <span class="number">0</span>:</span><br><span class="line">        ptimer.wait_for_tick()</span><br><span class="line">        print(<span class="string">"n="</span>, nticks)</span><br><span class="line">        nticks -= <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">countup</span><span class="params">(last)</span>:</span></span><br><span class="line">    n = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> n &lt; last:</span><br><span class="line">        ptimer.wait_for_tick()</span><br><span class="line">        print(<span class="string">"n="</span>, n)</span><br><span class="line">        n += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">Thread(target=countdown, args=(<span class="number">10</span>, )).start()</span><br><span class="line">Thread(target=countup, args=(<span class="number">5</span>, )).start()</span><br></pre></td></tr></table></figure><p>Event 对象的关键特性就是他会唤醒所有等待的线程。如果我们编写的程序只希望唤醒一个单独的等待线程，那么最好使用Semaphore或者Condition对象。</p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> 多线程 </tag>
            
            <tag> 并发 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Python的并发编程（一）-了解并发以及简单的多线程实例</title>
      <link href="/python-concurrent-1.html"/>
      <url>/python-concurrent-1.html</url>
      
        <content type="html"><![CDATA[<p>求职过程中，好多公司的招聘信息都会写一条：<strong>有构建大型互联网服务及高并发等经验</strong></p><p>那什么是高并发呢？</p><p><strong>对于服务端接口来说，就是我们的接口可以同时并行处理大量的请求。</strong></p><h3 id="理解并发与并行"><a href="#理解并发与并行" class="headerlink" title="理解并发与并行"></a>理解并发与并行</h3><p>先了解一下并发和并行的区别。百度百科对并发和并行的解释如下：</p><a id="more"></a><blockquote><p><strong>并发</strong>当有多个线程在操作时,如果系统只有一个CPU,则它根本不可能真正同时进行一个以上的线程，它只能把CPU运行时间划分成若干个时间段,再将时间 段分配给各个线程执行，在一个时间段的线程代码运行时，其它线程处于挂起状。.这种方式我们称之为并发(Concurrent)。</p><p><strong>并行：</strong>当系统有一个以上CPU时,则线程的操作有可能非并发。当一个CPU执行一个线程时，另一个CPU可以执行另一个线程，两个线程互不抢占CPU资源，可以同时进行，这种方式我们称之为并行(Parallel)。</p></blockquote><p>两者之间有什么区别呢，举个例子说明一下：</p><p>下面是ABC三座桥（画得比较丑），下面的例子只是为了帮助理解并行和并发的区别，不要太较真显示生活中怎么处理这样的情况。</p><ul><li>A桥：当有一辆车行驶在桥面上之后，如果对面来了车，只有等待桥上的车离开，对面的车才可以上桥并通过，这样可以理解为A桥不支持并发。</li><li>B桥：当有一辆车行驶在桥面上之后，如果对面来了车，第一辆车可以进入那块小区域，对面的车过去第一辆车再接着行驶，可以说B桥支持并发。</li><li>C桥：两条车道，自己走自己的，可以说C桥支持并行。</li></ul><p><img src="https://i.loli.net/2019/05/08/5cd2711d34fca.jpg" alt="img"></p><h3 id="Python中的并发"><a href="#Python中的并发" class="headerlink" title="Python中的并发"></a>Python中的并发</h3><p>Python也在很早就开始支持多种不同的并发编程方法，包括多线程，加载子进程以及各种涉及生成器函数的技巧（协程）。</p><h4 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h4><p>Python3提供了threading库来实现多线程，可用来在单独的线程中执行任意的Python可调用对象。</p><h5 id="启动和停止"><a href="#启动和停止" class="headerlink" title="启动和停止"></a>启动和停止</h5><p>先写一个简单的函数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">countdown</span><span class="params">(n)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> n &gt; <span class="number">0</span>:</span><br><span class="line">        print(<span class="string">"n="</span>, n)</span><br><span class="line">        n-=<span class="number">1</span></span><br><span class="line">        time.sleep(<span class="number">5</span>)</span><br></pre></td></tr></table></figure><p>使用多线程来执行上面的函数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line">t = Thread(target=countdown, args=(<span class="number">10</span>,))</span><br><span class="line">t.start()</span><br></pre></td></tr></table></figure><p>注意：当创建一个线程实例时，在调用它的<code>start()</code>方法之前，线程不会立即开始执行。<strong>args需要的参数为元组。</strong></p><p>线程实例会在他们所属的系统级线程（POSIX线程或Windows线程）中执行，这些线程完全由操作系统来管理，一旦启动之后，县城就开始独立运行，直到目标函数返回，可以使用<code>t.is_alive()</code>来判断线程是否还在运行。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    <span class="keyword">if</span> t.is_alive():</span><br><span class="line">        print(<span class="string">"执行中"</span>)</span><br><span class="line">        time.sleep(<span class="number">10</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        print(<span class="string">"完成"</span>)</span><br><span class="line">        <span class="keyword">break</span></span><br></pre></td></tr></table></figure><p>也可以使用：<code>t.join()</code>来连接到该线程，等待该线程执行结束。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">t.join()</span><br><span class="line">print(<span class="string">"完成"</span>)</span><br></pre></td></tr></table></figure><p>在线程执行的过程中，Python的解释器会一直保持运行，直到所有的线程都终结。对于需要长时间运行的线程或者一直不断运行的后台任务，应该考虑将这些线程设置为daemon（守护线程）,daemon是无法被连接的，但是在主线程结束之后他们会自动销毁。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">t = Thread(target=countdown, args=(<span class="number">10</span>,), daemon=<span class="literal">True</span>)</span><br><span class="line">t.start()</span><br></pre></td></tr></table></figure><p>除了以上展示的两种操作外，对于线程没有太多的操作可做了。比如终止线程，给线程发信号，调整线程调度属性以及执行任何其他的高级操作，如果需要，就得自己构建。</p><p>如果想要终止线程，这个线程必须要能够在某个点上轮询退出状态。我们可以试着实现一下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CountdownTask</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.__running = <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">terminate</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.__running = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self, n)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> self.__running <span class="keyword">and</span> n &gt; <span class="number">0</span>:</span><br><span class="line">            print(<span class="string">"n="</span>, n)</span><br><span class="line">            n -= <span class="number">1</span></span><br><span class="line">            time.sleep(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line">c = CountdownTask()</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line">t = Thread(target=c.run, args=(<span class="number">10</span>,))</span><br><span class="line">t.start()</span><br></pre></td></tr></table></figure><p>可以通过<code>c.terminate()</code>来终止线程。</p><h4 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h4><p>由于Python存在大名鼎鼎的全局解释器锁（GIL），所以Python的多线程其实只是伪多线程，解释器限制在任意时刻只允许运行一个线程。由于这个原因，不应该使用Python县城来处理计算密集型的任务，因为在这种任务中我们希望在多个CPU和行上实现<strong>并行</strong>处理。Python线程更适合I/O处理以及设计阻塞操作的并发执行任务（即，等待I/O，等待从数据库中取出结果等）。</p><p>有时候我们会发现从Thread类中继承而来的线程类。比如：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CountdownThread</span><span class="params">(Thread)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, n)</span>:</span></span><br><span class="line">        super().__init__()</span><br><span class="line">        self.n = n</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> self.n &gt; <span class="number">0</span>:</span><br><span class="line">            print(<span class="string">"n="</span>, n)</span><br><span class="line">            n -= <span class="number">1</span></span><br><span class="line">            time.sleep(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line">c = CountdownThread(<span class="number">5</span>)</span><br><span class="line">c.start()</span><br></pre></td></tr></table></figure><p>尽管这样也可以完成任务，但这个在代码和threading库之间引入了一层额外的依赖关系。意思就是说，上面的代码只能用在有关线程的上下文中，而我们之前展示的技术中编写的代码并不会依赖于threading库。</p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> 多线程 </tag>
            
            <tag> 并发 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>一起学习数据结构（一）-认识数据结构</title>
      <link href="/structure-1.html"/>
      <url>/structure-1.html</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>官方点说：</p><blockquote><p>数据结构是一门研究非数值计算的程序设计问题中的操作对象，以及他们之间的 关系和操作等相关问题的学科。</p></blockquote><p>作为一个程序员，肯定明白：<strong>程序设计 = 数据结构 + 算法</strong></p><p>想要写出优美的代码，设计出色的程序逻辑，所以数据结构和算法是必须要掌握的知识。我也在学习的过程中，在阅读的书籍是《大话数据结构》，会在这记录</p><h2 id="认识数据结构"><a href="#认识数据结构" class="headerlink" title="认识数据结构"></a>认识数据结构</h2><p>数据结构具体指什么呢？看看下面两个例子。</p><a id="more"></a><h3 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h3><p>每个元素都存在指针，指向了下一个元素。</p><p><img src="https://i.loli.net/2019/05/08/5cd2711c8a4cf.png" alt="链表"></p><h3 id="树"><a href="#树" class="headerlink" title="树"></a>树</h3><p>多个节点之间存在父子关系组成的结构，看起来像一棵倒立的树，所以称为树。</p><p><img src="https://i.loli.net/2019/05/08/5cd2711c26006.png" alt="tree"></p><p>多个元素相互之间存在特定的关系，组成了对应的数据结构。上面的两种结构只是简单介绍，以后会有详细的解释，此处只是为了告诉大家什么是数据结构。</p><blockquote><p><strong>是由多个存在特定关系的数据元素的集合，即是多个数据组成的特定结构</strong></p></blockquote><h2 id="什么是数据"><a href="#什么是数据" class="headerlink" title="什么是数据"></a>什么是数据</h2><p><strong>在计算机中，数据是可以被操作的对象，是能被计算机识别处理的。</strong>不仅仅指我们在编程过程中常用的整形，浮点型等类型，还包括文字，图片，音频，视频等。</p><p>所以在这里说的数据，是必备以下两种特性的符号：</p><ul><li>可以输入到计算机</li><li>可以被计算机程序处理。</li></ul><h2 id="数据是由什么组成的"><a href="#数据是由什么组成的" class="headerlink" title="数据是由什么组成的"></a>数据是由什么组成的</h2><p><strong>数据元素是组成数据的、有一定意义的基本单位。</strong></p><p>比如文本，是由多个字符组成的，视频图片等二进制文件是由多个二进制字节组成的。</p><h2 id="理解数据结构"><a href="#理解数据结构" class="headerlink" title="理解数据结构"></a>理解数据结构</h2><p><strong>结构，可以理解为关系</strong>。</p><p>比如一个公司，有上下级的关系，有多个部门，互相之间有合作竞争的关系，这就是一种结构。</p><p><strong>在计算机中，多个元素之间存在一种或者多种特定的关系组成的结构，就是数据结构。</strong></p>]]></content>
      
      
      <categories>
          
          <category> 数据结构 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 数据结构 </tag>
            
            <tag> 读书 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>leetcode-python-搜索二维矩阵</title>
      <link href="/leetcode-search-matrix.html"/>
      <url>/leetcode-search-matrix.html</url>
      
        <content type="html"><![CDATA[<blockquote><p>编写一个高效的算法来搜索 m x n 矩阵 matrix 中的一个目标值 target。该矩阵具有以下特性：<br>每行的元素从左到右升序排列。<br>每列的元素从上到下升序排列。<br>示例:<br>现有矩阵 matrix 如下：</p></blockquote><p>[</p><p>  [1,4,7,11,15],</p><p>  [2,5,8,12,9],</p><p>  [3,6,9,16,22],</p><p>  [10,13,14,17,24],</p><p>  [18,21,23,26,30],</p><p>]</p><p>给定 target = 5，返回 true。</p><p>给定 target = 20，返回 false。</p><a id="more"></a><p>使用的是遍历搜索的方式，根据矩阵的规则，写好跳出条件，减少循环次数。之后会用二叉搜索树再实现一次。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">search_matrix</span><span class="params">(matrix: list, target: int)</span> -&gt; bool:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    :type matrix: list</span></span><br><span class="line"><span class="string">    :type target: int</span></span><br><span class="line"><span class="string">    :rtype: bool</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> matrix:</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> i:</span><br><span class="line">            <span class="keyword">if</span> target &lt; j:</span><br><span class="line">                <span class="comment"># 当要搜索的数字小于当前数字的时候，结束本次遍历，减少遍历次数</span></span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            <span class="keyword">elif</span> target == j:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    m = [</span><br><span class="line">            [<span class="number">1</span>, <span class="number">4</span>, <span class="number">7</span>, <span class="number">11</span>, <span class="number">15</span>],</span><br><span class="line">            [<span class="number">2</span>, <span class="number">5</span>, <span class="number">8</span>, <span class="number">12</span>, <span class="number">19</span>],</span><br><span class="line">            [<span class="number">3</span>, <span class="number">6</span>, <span class="number">9</span>, <span class="number">16</span>, <span class="number">22</span>],</span><br><span class="line">            [<span class="number">10</span>, <span class="number">13</span>, <span class="number">14</span>, <span class="number">17</span>, <span class="number">24</span>],</span><br><span class="line">            [<span class="number">18</span>, <span class="number">21</span>, <span class="number">23</span>, <span class="number">26</span>, <span class="number">30</span>]</span><br><span class="line">        ]</span><br><span class="line">    t = <span class="number">20</span></span><br><span class="line">    print(search_matrix(m, t))</span><br></pre></td></tr></table></figure><p>刷题顺序是按照LeetCode的<a href="https://leetcode-cn.com/explore/interview/card/top-interview-quesitons-in-2018/" target="_blank" rel="noopener">算法面试题汇总</a>进行的。文章开始发布在简书，现迁移到自己的博客。</p>]]></content>
      
      
      <categories>
          
          <category> 算法 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> leetcode </tag>
            
            <tag> python </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>hexo配置评论功能</title>
      <link href="/hexo-con.html"/>
      <url>/hexo-con.html</url>
      
        <content type="html"><![CDATA[<h3 id="给博客增加评论以及阅读统计的功能"><a href="#给博客增加评论以及阅读统计的功能" class="headerlink" title="给博客增加评论以及阅读统计的功能"></a>给博客增加评论以及阅读统计的功能</h3><p>next主题支持挺多的评论系统的，我选择使用<a href="https://valine.js.org/" target="_blank" rel="noopener">valine</a>，无后端以及集成了文章阅读量统计。</p><a id="more"></a><h4 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h4><p><a href="https://leancloud.cn" target="_blank" rel="noopener">注册</a>，创建开发版的应用。</p><p>在设置&gt;应用key中复制自己appid和appkey。</p><p><img src="https://i.loli.net/2019/06/08/5cfbd4a85d79f98056.png" alt></p><h4 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h4><p>在主题配置文件中搜索valine。修改enable为true，打开评论系统，将你的appid和appkey填入配置中。visitor为文章阅读数量的统计，阅读数以及评论会保存在leancloud中。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Valine</span></span><br><span class="line"><span class="comment"># You can get your appid and appkey from https://leancloud.cn</span></span><br><span class="line"><span class="comment"># More info available at https://valine.js.org</span></span><br><span class="line"><span class="attr">valine:</span></span><br><span class="line"><span class="attr">  enable:</span> <span class="literal">true</span> <span class="comment"># When enable is set to be true, leancloud_visitors is recommended to be closed for the re-initialization problem within different leancloud adk version.</span></span><br><span class="line"><span class="attr">  appid:</span>  <span class="string">你的appid</span></span><br><span class="line"><span class="attr">  appkey:</span>  <span class="string">你的appkey</span></span><br><span class="line"><span class="attr">  notify:</span> <span class="literal">false</span> <span class="comment"># mail notifier, See: https://github.com/xCss/Valine/wiki</span></span><br><span class="line"><span class="attr">  verify:</span> <span class="literal">false</span> <span class="comment"># Verification code</span></span><br><span class="line"><span class="attr">  placeholder:</span> <span class="string">请留下你的看法或者疑问</span> <span class="comment"># comment box placeholder</span></span><br><span class="line"><span class="attr">  avatar:</span> <span class="string">mm</span> <span class="comment"># gravatar style</span></span><br><span class="line"><span class="attr">  guest_info:</span> <span class="string">nick,mail</span> <span class="comment"># custom comment header</span></span><br><span class="line"><span class="attr">  pageSize:</span> <span class="number">10</span> <span class="comment"># pagination size</span></span><br><span class="line"><span class="attr">  language:</span> <span class="string">zh-cn</span></span><br><span class="line"><span class="attr">  visitor:</span> <span class="literal">true</span> <span class="comment"># leancloud-counter-security is not supported for now. When visitor is set to be true, appid and appkey are recommended to be the same as leancloud_visitors' for counter compatibility. Article reading statistic https://valine.js.org/visitor.html</span></span><br><span class="line"><span class="attr">  comment_count:</span> <span class="literal">true</span> <span class="comment"># if false, comment count will only be displayed in post page, not in home page</span></span><br></pre></td></tr></table></figure><h4 id="记录我安装的插件"><a href="#记录我安装的插件" class="headerlink" title="记录我安装的插件"></a>记录我安装的插件</h4><ul><li>使用git部署</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure><ul><li>提交百度</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install hexo-baidu-url-submit --save</span><br></pre></td></tr></table></figure><ul><li>生成网站地图</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">npm install hexo-generator-sitemap --save  <span class="comment"># 生成网站地图</span></span><br><span class="line">npm install hexo-generator-baidu-sitemap --save  <span class="comment"># 生成百度需要的网站地图</span></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 杂项 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 博客部署 </tag>
            
            <tag> hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>leetcode-python-只出现一次的数字</title>
      <link href="/single-num.html"/>
      <url>/single-num.html</url>
      
        <content type="html"><![CDATA[<blockquote><p>给定一个非空整数数组，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。<br>说明：<br>你的算法应该具有线性时间复杂度。 你可以不使用额外空间来实现吗？<br>示例 1:<br>输入: [2,2,1]<br>输出: 1<br>示例 2:<br>输入: [4,1,2,1,2]<br>输出: 4</p></blockquote><a id="more"></a><p>题目要求不使用额外的空间来实现，所以使用位运算符实现，关于位运算符：<br><img src="https://i.loli.net/2019/08/14/nHARBmt2qMhawXG.png" alt="image.png"><br>详细的请看菜鸟教程中的解释<a href="http://www.runoob.com/python/python-operators.html" target="_blank" rel="noopener">http://www.runoob.com/python/python-operators.html</a></p><p>代码如下</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">single_num</span><span class="params">(nums: list)</span> -&gt;int:</span></span><br><span class="line">    a = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> nums:</span><br><span class="line">        a = a ^ i</span><br><span class="line">    <span class="keyword">return</span> a</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    n = [<span class="number">2</span>, <span class="number">2</span>, <span class="number">1</span>]</span><br><span class="line">    print(single_num(n))</span><br></pre></td></tr></table></figure><p>刷题顺序是按照LeetCode的<a href="https://leetcode-cn.com/explore/interview/card/top-interview-quesitons-in-2018/" target="_blank" rel="noopener">算法面试题汇总</a>进行的.</p>]]></content>
      
      
      <categories>
          
          <category> 算法 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> leetcode </tag>
            
            <tag> python </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>深入python内存管理</title>
      <link href="/python-memory-management.html"/>
      <url>/python-memory-management.html</url>
      
        <content type="html"><![CDATA[<p>面试中被问到python的内存管理，只是说是python有自己的内存管理机制，有自己的垃圾回收机制，却不能详细作答，面试官表示很遗憾。建议我代码的业务逻辑需要想，但是学习需要深入底层，也有助于扩宽自己的知识面，对自己之后的学习路径有帮助，哈哈，感谢面试官帮我指出自己的不足。</p><p>回家马上查资料，先解决这个问题。</p><p>首先看看各种python常见面试题上的答案：</p><blockquote><p>python内存管理是由私有堆空间管理的，所有的python对象和数据结构都存储在私有堆空间中。程序员没有访问堆的权限，只有解释器才能操作。为python的堆空间分配内存的是python的内存管理模块进行的，核心api会提供一些访问该模块的方法供程序员使用。python自有的垃圾回收机制回收并释放没有被使用的内存供别的程序使用。</p></blockquote><p>如果仅仅问道这，上面的答案也足够了，但是面试官想要了解到更多，可能会衍生一些别的问题，那上面的答案就不够了。</p><a id="more"></a><p>以下内容： 作者：Vamei 出处：<a href="http://www.cnblogs.com/vamei" target="_blank" rel="noopener">http://www.cnblogs.com/vamei</a> 欢迎转载，也请保留这段声明。谢谢！</p><p>语言的内存管理是语言设计的一个重要方面。他是决定语言性能的重要因素。无论是C的手动管理还是java的垃圾回收，都成为语言重要的特征。下面已python语言为例子，说明一门动态类型的面向对象的语言的内存管理方式。</p><hr><h3 id="对象的内存使用"><a href="#对象的内存使用" class="headerlink" title="对象的内存使用"></a>对象的内存使用</h3><p>赋值语句是语言最常见的功能了。但即使是最简单的赋值语句，也可以很有内涵. 首先看看python的赋值语句：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">a = <span class="number">1</span></span><br></pre></td></tr></table></figure><p>整数“1”为一个对象，存储在内存空间中。a是一个引用。利用赋值语句，将引用a指向对象1。Python是动态类型的语言，对象与引用分离。文章作者比较形象的解释就是：Python像使用“筷子”那样，通过引用来接触和翻动真正的食物——对象。</p><p><strong>下面就是一系列的实验了，建议亲自尝试</strong> 可以通过python的内置函数id()，来探索对象在内存的存储。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;</span><span class="bash">&gt;&gt; a = 1</span></span><br><span class="line"><span class="meta">&gt;</span><span class="bash">&gt;&gt; id(a)</span></span><br><span class="line">140035503539424  # 内存地址的十进制表示</span><br><span class="line"><span class="meta">&gt;</span><span class="bash">&gt;&gt; hex(id(a))</span></span><br><span class="line">'0x7f5c8e71c4e0'  # 内存地址的十六进制表示</span><br></pre></td></tr></table></figure><p>在python中整数和短小的字符，python都会缓存这些对象，以便重复使用，当我们创建多个等于1的引用的时候，实际是让所有引用都指向同一个对象：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;</span><span class="bash">&gt;&gt; b = 1</span></span><br><span class="line"><span class="meta">&gt;</span><span class="bash">&gt;&gt; id(b)</span></span><br><span class="line">140035503539424  # 等于上面id(a)的值</span><br></pre></td></tr></table></figure><p>对比可以看出a和b实际是指向同一个对象的不同引用。 为了校验两个引用指向同一个对象，我们可以用“is”来判断。is用于判断两个引用所指的对象是否相同。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">a = <span class="number">1</span></span><br><span class="line">b = <span class="number">1</span></span><br><span class="line">print(a <span class="keyword">is</span> b)</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"></span><br><span class="line">a = <span class="string">"good"</span></span><br><span class="line">b = <span class="string">"good"</span></span><br><span class="line">print(a <span class="keyword">is</span> b)</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"></span><br><span class="line">a = <span class="string">"very good morning"</span></span><br><span class="line">b = <span class="string">"very good morning"</span></span><br><span class="line">print(a <span class="keyword">is</span> b)</span><br><span class="line"><span class="literal">True</span>   <span class="comment"># 原文在此处就是False，但是我的为True，通过查资料发现是python版本原因</span></span><br><span class="line"><span class="comment"># Python2.3简单整数缓存范围是(-1,100)，Python2.5.4以后简单整数缓存范围至少是(-5,256)。所有的短字符也都在缓存区。</span></span><br><span class="line"></span><br><span class="line">a = <span class="string">"为了校验两个引用指向同一个对象，我们可以用“is”来判断。is用于判断两个引用所指的对象是否相同。"</span></span><br><span class="line">b = <span class="string">"为了校验两个引用指向同一个对象，我们可以用“is”来判断。is用于判断两个引用所指的对象是否相同。"</span></span><br><span class="line">print(a <span class="keyword">is</span> b)</span><br><span class="line"><span class="literal">False</span>  <span class="comment"># 增加了字符串的长度，结果也是False</span></span><br><span class="line"></span><br><span class="line">a = []</span><br><span class="line">b = []</span><br><span class="line">print(a <span class="keyword">is</span> b)</span><br><span class="line"><span class="literal">False</span></span><br></pre></td></tr></table></figure><p>根据上面的运行结果，可以看到由于python缓存了整数和短字符串，因此每个对象只存有一份。比如所有的1的引用都指向同一对象。即使使用赋值语句，也只是创造了新的引用，而不是对象本身，长的字符串和其他对象可以有多个相同对象，可以使用赋值语句创建出新的对象。</p><p>在python中，每个对象都有存有指向该对象的应用总数，即<strong><em>引用计数(reference count)</em></strong> 呢 我们可以使用<code>sys</code>包中的<code>getrefcount()</code>，来查看某个对象的引用计数。需要注意的是，当使用某个引用作为参数，传递给<code>getrefcount()</code>时，会创建一个临时引用，所以结果会比预期多1。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> sys <span class="keyword">import</span> getrefcount</span><br><span class="line"></span><br><span class="line">a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">print(getrefcount(a))</span><br><span class="line"><span class="comment"># 2</span></span><br><span class="line">b = a</span><br><span class="line">print(getrefcount(b))</span><br><span class="line"><span class="comment"># 3</span></span><br></pre></td></tr></table></figure><p>由于上述原因，<code>getrefcount()</code>返回的结果分别是2,3，而不是期望的1。</p><hr><h3 id="对象引用对象"><a href="#对象引用对象" class="headerlink" title="对象引用对象"></a>对象引用对象</h3><p>python的一个容器对象（container），比如列表字典等，可以包含多个对象。实际上，容器对象中包含的并不是对象本身，而是指向各个元素对象的引用。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">from_obj</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, to_obj)</span>:</span></span><br><span class="line">        self.to_obj = to_obj</span><br><span class="line">b = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line">a = from_obj(b)</span><br><span class="line">print(id(a.to_obj))</span><br><span class="line">print(id(b))</span><br><span class="line"><span class="comment"># 140035473779144</span></span><br><span class="line"><span class="comment"># 140035473779144</span></span><br></pre></td></tr></table></figure><p>可以看到a引用了对象b。</p><p>对象引用对象是python最基本的构成方式。即使是a = 1这一赋值方式，实际上是让词典的一个键值“a”的元素引用整数对象1。该词典对象用于记录所有的全局引用。该词典引用了整数对象1。我们可以通过内置函数<code>globals()</code>来查看该词典。 当一个对象a被另一个对象b引用时，a的引用计数将增加1。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> sys <span class="keyword">import</span> getrefcount</span><br><span class="line">a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">print(getrefcount(a))</span><br><span class="line">b = [a, a]</span><br><span class="line">print(getrefcount(a))</span><br><span class="line"><span class="comment"># 2</span></span><br><span class="line"><span class="comment"># 4</span></span><br></pre></td></tr></table></figure><p>由于对象b引用了a两次，所以a的引用计数加2。</p><p>容器对象引用可能构成很复杂的拓扑结构。我们可以用objgraph包来绘制其引用关系。 objgraph是python的一个第三方包。<a href="http://mg.pov.lt/objgraph/" target="_blank" rel="noopener">objgraph官网</a>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pip install objgraph</span><br></pre></td></tr></table></figure><p> 使用objgraph需要安装xdot。根据自己的系统发行版本安装。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo pacman -S xdot或者 sudo apt install xdot，sudo yun install xdot</span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">x = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">y = [x, dict(key1=x)]</span><br><span class="line">z = [y, (x, y)]</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> objgraph</span><br><span class="line">objgraph.show_refs([z], filename=<span class="string">'ref_topo.png'</span>)</span><br></pre></td></tr></table></figure><p><img src="https://i.loli.net/2019/08/14/Tlr7paBWn3y9jGQ.png" alt="ref_topo.png"></p><p>两个对象可能互相引用，从而构成所谓的引用环（reference cycle）</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = []</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [a]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(b)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[[[...]]]</span><br></pre></td></tr></table></figure><p>即使是一个对象，只需要自己引用自己，也能构成引用环。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = []</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(getrefcount(a))</span><br><span class="line"><span class="number">3</span></span><br></pre></td></tr></table></figure><p>引用环会给垃圾回收机制带来很大的麻烦，我将在后面详细叙述这一点。</p><hr><h3 id="引用减少"><a href="#引用减少" class="headerlink" title="引用减少"></a>引用减少</h3><p>某个引用对象的引用计数可能减少。比如使用del关键字删除某个引用</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(getrefcount(b))</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">del</span> a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(getrefcount(b))</span><br><span class="line"><span class="number">2</span></span><br></pre></td></tr></table></figure><p>del也可以删除容器中的元素，比如：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">del</span> a[<span class="number">0</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(a)</span><br><span class="line">[<span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = &#123;<span class="string">"q"</span>: <span class="number">1</span>, <span class="string">"w"</span>:<span class="number">2</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">&#123;<span class="string">'q'</span>: <span class="number">1</span>, <span class="string">'w'</span>: <span class="number">2</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">del</span> b[<span class="string">"q"</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">&#123;<span class="string">'w'</span>: <span class="number">2</span>&#125;</span><br></pre></td></tr></table></figure><p>如果某个引用指向对象a，当这个引用被重新定向到其他对象b的时候，对象a的引用计数会减少</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> sys <span class="keyword">import</span> getrefcount</span><br><span class="line">...</span><br><span class="line"><span class="meta">... </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">... </span>b = a</span><br><span class="line"><span class="meta">... </span>print(getrefcount(b))</span><br><span class="line">...</span><br><span class="line"><span class="meta">... </span>a = <span class="number">1</span></span><br><span class="line"><span class="meta">... </span>print(getrefcount(b))</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="number">2</span></span><br></pre></td></tr></table></figure><hr><h3 id="垃圾回收"><a href="#垃圾回收" class="headerlink" title="垃圾回收"></a>垃圾回收</h3><p>当python中的对象越来越多，他占据的内存也会越来越大。不过不需要担心太多，python会在适当的时候启动垃圾<code>回收机制(garbage collection)</code>，将没用的对象清除，在许多语言中都有垃圾回收机制，比如Java和Ruby。</p><p>从基本原理来说，当一个对象的引用计数降为0的时候，说明没有任何引用指向对象，这时候该对象就成为需要被清除的垃圾了。比如某个新建对象，分配给某个引用，引用数为1,当引用被删除之后，引用数为0，那么该对象就可以被垃圾回收。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="keyword">del</span> a</span><br></pre></td></tr></table></figure><p><code>del a</code>之后已经没有任何引用指向[1,2,3]了，用户不可能通过任何方式接触或者动用这个对象，这个对象如果继续待在内存里，就成了不健康的数据。当python的<strong><em>垃圾回收</em></strong>机制启动的时候，python扫描到这个引用为0的对象，就会将它所占据的内存清空。</p><p>然而清理过程是个费力的过程。垃圾回收的时候，python不能进行其他的任务，频繁的垃圾回收，会大大降低python的工作效率。如果内存中的对象不多，就没必要总启动垃圾回收。所以python只会在特定的条件下，<strong><em>自动启动垃圾回收</em></strong>。当python运行的时候，会记录其中分配对象和取消分配对象的次数，两者的差值高于某个阈值的时候，垃圾回收才会启动。 我们可以通过gc模块的<code>get_threshold()</code>来查看该阈值。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> gc</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>gc.get_threshold()</span><br><span class="line">(<span class="number">700</span>, <span class="number">10</span>, <span class="number">10</span>)</span><br></pre></td></tr></table></figure><p>返回值中，后面的两个10,是与分代回收相关的阈值，分代回收在后面会讲到。700既是垃圾回收的启动阈值。可以通过gc中的<code>set_threshold()</code>来重新设定。 也可以手动使用<code>gc.collect()</code>启动垃圾回收机制。</p><hr><h3 id="分代回收"><a href="#分代回收" class="headerlink" title="分代回收"></a>分代回收</h3><p>python同时使用了分代（generation）回收的策略。这一策略的基本假设是，存活时间越久的对象，越不可能在后面的程序中变成垃圾。我们的程序往往会产生大量的对象，许多对象很快产生和消失，但也有长期存在被使用的对象，出于信任和效率，对于这样一些对象，我们相信他的用处，所以减少在垃圾回收中扫描他们的频率。</p><p>python将所有的对象分为0,1,2三代，所有新建的对象都是0代对象，当某一代对象经历过垃圾回收之后，依然存活，那就归入到下一代中，垃圾回收启动时，一定会扫描所有的0代对象。如果0代对象经历过一定次数的垃圾回收，那么就启动对0待和1代的扫描清理，当1代也经历了一定数量的垃圾回收，那就启动对0,1,2，即所有的对象进行扫描。</p><p>上面<code>gc.get_threshold()</code>返回的（700,10,10）中后面的两个数，意义就是，每经过10次对0代的垃圾回收，就会配合启动一次对1代的扫描，没经过10次对1代的扫描，才会启动一次对2代的垃圾回收。</p><p>同样可以用set_threshold()来调整，比如对2代对象进行更频繁的扫描。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> gc</span><br><span class="line">gc.set_threshold(<span class="number">700</span>, <span class="number">10</span>, <span class="number">5</span>)</span><br></pre></td></tr></table></figure><hr><h3 id="孤立的引用环"><a href="#孤立的引用环" class="headerlink" title="孤立的引用环"></a>孤立的引用环</h3><p>引用环的存在会给垃圾回收带来很大的困难，这些引用环可能构成无法使用，但是引用计数不为0的一些对象。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">a = []</span><br><span class="line">b = [a]</span><br><span class="line">a.append(b)</span><br><span class="line"><span class="keyword">del</span> a</span><br><span class="line"><span class="keyword">del</span> b</span><br></pre></td></tr></table></figure><p>上面我们先创建了两个表对象，并引用对方，构成一个引用环。删除了a，b引用之后，这两个对象不可能再从程序中调用，就没有什么用处了。但是由于引用环的存在，这两个对象的引用计数都没有降到0，不会被垃圾回收。 <img src="https://i.loli.net/2019/08/14/KZiDkvBopAafMHh.png" alt="孤立的引用环"></p><p>为了回收这样的引用环，Python复制每个对象的引用计数，可以记为gc_ref。假设，每个对象i，该计数为gc_ref_i。Python会遍历所有的对象i。对于每个对象i引用的对象j，将相应的gc_ref_j减1。 <img src="https://i.loli.net/2019/08/14/SezEXqxlitfMA1P.png" alt="遍历后的结果"> 在结束遍历后，gc_ref不为0的对象，和这些对象引用的对象，以及继续更下游引用的对象，需要被保留。而其它的对象则被垃圾回收。</p><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>python作为一种动态类型的语言，其对象和引用分离，这与面向过程的编程语言有很大的区别。为了有效的释放内存，python内置了垃圾回收的支持。python采用了一种相对简单的垃圾回收机制，即引用计数，并因此需要解决孤立引用环的问题。Python与其它语言既有共通性，又有特别的地方。对该内存管理机制的理解，是提高Python性能的重要一步。</p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> python </tag>
            
            <tag> 面试 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>使用Hexo部署自己的博客</title>
      <link href="/hexo.html"/>
      <url>/hexo.html</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>找工作两个月了，面试了大大小小近十家，面试机会真的是太少了，我以为可以一天面两家的，哈哈，又不怕累，但是并没有给我这个机会。</p><p>在投简历的过程中，发现好多jd上都建议附个人博客的地址，算是加分项，反观自己17年9月开始工作，大大小小的问题遇到的不少，却没有这方面的积累，应该说是直接没有这种想法。</p><p>还好，不算晚，我还年轻。</p><h2 id="为什么使用Hexo来搭建博客"><a href="#为什么使用Hexo来搭建博客" class="headerlink" title="为什么使用Hexo来搭建博客"></a>为什么使用Hexo来搭建博客</h2><p>当然作为一个后端程序员，还是python开发，一开始想着的是自己用Django框架可以快速的写一个简单的博客。确实很快，博客列表和详情一早上就搞定了，前端使用的一个简单的模板，修修改改，样式不是很喜欢，但是可选项不多，谁让我自己不会设计，不会写前端呢。没事，可以用就好了，博客最重要的是内容，又不是外表，只能这么安慰自己。</p><p>然后求职过程中接到了一个外包，博客就搁浅了，好多想要的功能还没开始写，还是要把接到的任务放在第一位的，毕竟要吃饭的嘛。</p><p>写完任务之后，心态有点变了，我在找工作，我还要吃饭，至少工作没稳定之前没有那么多时间，就决定使用现有的博客系统来完成，然后就查一些优劣，应该选择哪种方案，参考了以下文章。</p><a id="more"></a><p><a href="https://www.zhihu.com/question/24179143" target="_blank" rel="noopener">准备自己建一个个人博客，有什么好的框架推荐？</a></p><p><a href="http://git.bookislife.com/post/2015/personal-blog-choosen/" target="_blank" rel="noopener">个人博客平台的选择</a></p><p>结合自己目前的需求，最后选择了Hexo。</p><h2 id="Hexo"><a href="#Hexo" class="headerlink" title="Hexo"></a>Hexo</h2><p>分为介绍和部署两部分。</p><p><strong><em>介绍部分内容在文档中都有，我只是按流程做一个简单的教程，以及遇到的问题，详细的建议仔细阅读官方文档。</em></strong></p><h3 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h3><p>Hexo是由中国人写的，有着中文文档的支持。</p><p><a href="https://hexo.io/zh-cn/" target="_blank" rel="noopener">hexo中文官网</a>是这么介绍的：</p><blockquote><p>Hexo 是一个快速、简洁且高效的博客框架。Hexo 使用 Markdown（或其他渲染引擎）解析文章，在几秒内，即可利用靓丽的主题生成静态网页。</p></blockquote><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><h4 id="依赖"><a href="#依赖" class="headerlink" title="依赖"></a>依赖</h4><ul><li><p>node.js(需要6.9版本以上)</p><p>  根据自己的系统环境安装，官网有对应的安装包，Linux可以直接通过命令行安装。</p></li><li><p>git</p><p>  Windows：下载并安装 <a href="https://git-scm.com/download/win" target="_blank" rel="noopener">git</a>.</p><p>  Mac：使用 Homebrew, MacPorts ：brew install git;或下载 <a href="https://sourceforge.net/projects/git-osx-installer/" target="_blank" rel="noopener">安装程序</a> 安装。</p><p>  Linux：使用对应的Linux的发行版本的安装命令安装。</p></li></ul><h4 id="安装Hexo"><a href="#安装Hexo" class="headerlink" title="安装Hexo"></a>安装Hexo</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure><p>命令很简单，但是在我运行的时候报错了，报错两次。（讲道理这种情况比较少出现）</p><ul><li>第一次是报<code>/usr/lib/node_modules/</code>的操作权限。</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo chown -R `whoami` /usr/<span class="built_in">local</span>/lib/node_modules</span><br></pre></td></tr></table></figure><ul><li>第二次是在创建<code>/usr/bin/hexo</code>软链接的时候报错了。</li></ul><p>可以选择自己手动创建软链接，或者直接使用<code>sudo</code>命令安装，我选择<code>sudo</code>。（网上不建议这么做，怕安装的npm还是node环境多了有奇怪的异常，但是我是python开发啊，应该不会安装太多的这些环境）。</p><h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>我要在<code>/home/zhi/hexo</code>下创建一个博客站点。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> /home/zhi/hexo <span class="comment"># hexo文件夹是我自己新建的，方便管理所有使用hexo创建的站点。目前是空的。</span></span><br><span class="line"><span class="comment"># 初始换</span></span><br><span class="line">hexo init blog <span class="comment"># blog是我需要创建的站点名字，会在当前目录下生成</span></span><br><span class="line">ls</span><br><span class="line">blog <span class="comment"># 生成了对应名字的文件夹</span></span><br></pre></td></tr></table></figure><p>blog目录里面生成了需要的文件</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">.</span><br><span class="line">├── _config.yml <span class="comment"># 我们主要会做修改的配置文件</span></span><br><span class="line">├── db.json</span><br><span class="line">├── node_modules</span><br><span class="line">├── package.json</span><br><span class="line">├── package-lock.json</span><br><span class="line">├── public <span class="comment"># 生成的静态页面会在这</span></span><br><span class="line">├── scaffolds <span class="comment"># 模板</span></span><br><span class="line">├── <span class="built_in">source</span> <span class="comment"># 生成的md文章</span></span><br><span class="line">└── themes <span class="comment"># 主题</span></span><br></pre></td></tr></table></figure><h4 id="新建文章"><a href="#新建文章" class="headerlink" title="新建文章"></a>新建文章</h4><p>新建test的文章</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">hexo new <span class="built_in">test</span></span><br></pre></td></tr></table></figure><p>在source下会生成名为test.md的文件，编写自己的博客内容。</p><h4 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h4><p>hexo的配置文件为_config.yml，根据自己的需要进行配置。建议阅读hexo的文档，就不在这里赘述了。</p><h3 id="部署"><a href="#部署" class="headerlink" title="部署"></a>部署</h3><p>官方文档提供了几种不同的部署方式，较多的人部署在了github上面。由于github在国外，访问速度大多数情况下比较感人，如果有自己的服务器的话，可以选择部署在自己的服务器上。</p><p>我在这里选择使用nginx作为服务器，部署在了<a href="https://promotion.aliyun.com/ntms/yunparter/invite.html?userCode=4xhnc6a1" target="_blank" rel="noopener">阿里的云服务器中</a>，学生机比较实惠。</p><h3 id="方案"><a href="#方案" class="headerlink" title="方案"></a>方案</h3><p>虽然可以将public中的内容复制到服务器中，使用nginx代理静态文件，但是过程肯定是比较繁琐的，我们需要本地修改了，然后可以同步到服务器中.</p><p><img src="/images/blog_flow.png" alt="flow"></p><h3 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h3><h4 id="个人PC"><a href="#个人PC" class="headerlink" title="个人PC"></a>个人PC</h4><p>在之前的过程中，安装了hexo，还需要安装部署插件。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure><h4 id="服务器环境"><a href="#服务器环境" class="headerlink" title="服务器环境"></a>服务器环境</h4><p>我的服务器系统环境是Centos7.4.</p><p>安装Git</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">yum install git -y</span><br></pre></td></tr></table></figure><p>新建 Git 用户</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">useradd -m git</span><br></pre></td></tr></table></figure><p>设置 gituser 的密码</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">passwd git</span><br></pre></td></tr></table></figure><p>配置ssh免密登录服务器</p><ol><li><p>生成ssh密钥。</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen</span><br></pre></td></tr></table></figure></li><li><p>将公钥添加到服务器上。user为用户，host为服务器公网IP。</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-copy-id -i ~/.ssh/id_rsa.pub user@host</span><br></pre></td></tr></table></figure></li><li><p>验证是否成功</p> <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh user@host</span><br></pre></td></tr></table></figure></li></ol><p>初始化一个Git仓库。</p><p>新建 /var/repo 目录（位置并不是必须在这），并在该目录下，使用 git init –bare 创建一个名为 blog.git（名字可以自己修改，将以下的对应的内容都做修改就好） 裸仓库，并改变该目录的所有者为 git 用户。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">mkdir -p /var/repo</span><br><span class="line"><span class="built_in">cd</span> /var/repo</span><br><span class="line">git init --bare blog.git</span><br><span class="line">chown -R git:git blog.git</span><br></pre></td></tr></table></figure><p>看看生成的blog.git内的内容</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">blog.git</span><br><span class="line">├── branches</span><br><span class="line">├── config</span><br><span class="line">├── description</span><br><span class="line">├── HEAD</span><br><span class="line">├── hooks <span class="comment"># 接下来需要在此处进行一些配置</span></span><br><span class="line">├── index</span><br><span class="line">├── info</span><br><span class="line">├── objects</span><br><span class="line">└── refs</span><br></pre></td></tr></table></figure><p>配置Git hooks</p><p>进入上面生成的hooks目录，新建名为post-receive的文件，写入下面内容。</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/sh</span></span><br><span class="line">git --work-tree=/home/www/hexo --git-dir=/var/repo/blog.git checkout -f</span><br></pre></td></tr></table></figure><p>将post-receive设置为可执行。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">chmod +x /var/repo/blog.git/hooks/post-receive</span><br></pre></td></tr></table></figure><p>安装nginx</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">yum install nginx</span><br></pre></td></tr></table></figure><p>创建需要代理的文件夹，就是你问博客内容需要放在哪。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mkdir -p /home/www/hexo      //创建目录</span><br><span class="line">chown -R git:git /home/www/hexo   //更改目录所有者</span><br></pre></td></tr></table></figure><p>修改配置文件<code>/etc/nginx/nginx.conf</code>中以下的内容。将root对应的文件夹改为你上面创建的文件夹。</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       <span class="number">80</span> default_server;</span><br><span class="line">    <span class="attribute">listen</span>       [::]:<span class="number">80</span> default_server;</span><br><span class="line">    <span class="attribute">server_name</span>  _;</span><br><span class="line">    <span class="attribute">root</span>         /home/www/hexo;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">include</span> /etc/nginx/default.d/<span class="regexp">*.conf</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">location</span> / &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">error_page</span> <span class="number">404</span> /<span class="number">404</span>.html;</span><br><span class="line">        <span class="attribute">location</span> = /40x.html &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">error_page</span> <span class="number">500</span> <span class="number">502</span> <span class="number">503</span> <span class="number">504</span> /50x.html;</span><br><span class="line">        <span class="attribute">location</span> = /50x.html &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>配置<code>_config.yml</code>文件。找到deploy选项进行修改。</p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Deployment</span></span><br><span class="line"><span class="attr">deploy:</span></span><br><span class="line"><span class="attr">- type:</span> <span class="string">git</span></span><br><span class="line"><span class="attr">  repo:</span> <span class="string">your_user_name@HostIP:/var/repo/blog.git</span></span><br><span class="line"><span class="attr">  branch:</span> <span class="string">master</span></span><br><span class="line"><span class="attr">  message:</span></span><br></pre></td></tr></table></figure><p>发布自己的博客。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo d -g</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 杂项 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 博客部署 </tag>
            
            <tag> hexo </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
